# Handoff: nb4.1 — Event Studies & Parallel Trends (CoderAgent)

- Built `notebooks/week-04/nb4.1-event-study-parallel-trends.ipynb` (25 cells, 13 md + 12 code; rng seed 42, matplotlib Agg, outputs empty). Verified by extracting code to temp .py and running python3 → exit 0; all 7 assert-based CHECKs pass: double-diff == TWFE beta (68.85, to 1e-7), injected $12/yr pre-trend tilts leads (slope +11.4) and inflates static beta 68.85→128.85, low-power demo (lead-CI half-width 2.3→36 with noise), BDM placebo (classical over-rejects 25% under AR(1) ρ=0.6; clustering with 1 treated cluster does NOT rescue → 72%; permutation p=0.000). Reproduces the chapter's illustrative ATT=$70.
- pyfixest (0.50.0) vs statsmodels: both return the identical TWFE beta. pyfixest is far more convenient for panels (`premium ~ D | state + year` absorbs FE, `vcov={"CRV1":"state"}` clusters in one arg, `.tidy()`/`.confint()` give ready-to-plot coefs+CIs); statsmodels `C(state)+C(year)` dummies kept as the transparent fallback and as an independent cross-check of beta.
- Key DGP/pedagogy note for downstream agents: single treated state (faithful to Priya/Card-Krueger) means CRV1 clustered SE is unreliably SMALL/erratic (few-treated-clusters pathology) — notebook teaches this explicitly rather than asserting the textbook many-cluster direction. Added a `rho` AR(1) knob to `build_panel` so the placebo experiment shows genuine serial-correlation over-rejection. "Your Turn" extends via pre-trend slope sweep and varying number of treated states (same single treatment time — NOT staggered, that's 4.2).
