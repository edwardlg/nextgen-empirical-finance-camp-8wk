# Handoff w3-ch31 — WriterAgent (Ch 3.1: Potential Outcomes, SUTVA, Fundamental Problem)

- Word count: 6,168 (`wc -w`), target ~6,400 — within range. File: `book/weeks/week-03/ch31-potential-outcomes-sutva.md`. FOUNDATION chapter for Week 3; reveal-the-trick structure; anchored on Maya's loan-approval/financial-literacy puzzle (62% vs 41% = 21pt naive gap, decomposed to ATT 0.07 + selection bias 0.14). Builds explicitly on Week 2: ties selection bias to OVB, "controlling for X" = conditional independence as a *bet* (the Week-2 cliff), randomization needs no such assumption. Forward-refs every Week-3 chapter to "the one mission: zero the selection-bias term."
- Terms DEFINED here (locked notation — Ch 3.2–3.5 MUST reuse, NOT redefine): potential outcomes $Y_i(1),Y_i(0)$; treatment indicator $D_i\in\{0,1\}$; observation/consistency rule $Y_i=D_iY_i(1)+(1-D_i)Y_i(0)$; individual treatment effect $\tau_i=Y_i(1)-Y_i(0)$; Fundamental Problem of Causal Inference (Holland 1986) / missing counterfactual / causal inference = missing-data problem; estimand vs estimator vs estimate (recalled from Ch 1.3); **ATE** $=\mathbb{E}[\tau_i]$; **ATT** $=\mathbb{E}[\tau_i\mid D_i=1]$ (note its hidden $\mathbb{E}[Y_i(0)\mid D_i=1]$ counterfactual term); **LATE** (NAMED ONLY — full derivation deferred to Ch 3.4 IV / compliers); naive difference in means $\Delta=\mathbb{E}[Y_i\mid D_i=1]-\mathbb{E}[Y_i\mid D_i=0]$; **master decomposition** $\Delta=\text{ATT}+\text{selection bias}$ with selection bias $=\mathbb{E}[Y_i(0)\mid D_i=1]-\mathbb{E}[Y_i(0)\mid D_i=0]$ (derived in full); **SUTVA** (Part 1 no-interference / spillovers + general equilibrium; Part 2 no-hidden-versions); independence $(Y_i(0),Y_i(1))\perp\!\!\!\perp D_i$ under randomization → selection bias = 0, ATT=ATE, RCT = gold standard; **conditional independence / unconfoundedness / selection on observables** $(Y_i(0),Y_i(1))\perp\!\!\!\perp D_i\mid X_i$ (tees up Ch 3.2). Rubin causal model / Neyman attribution given. Science-table thought experiment (5-unit table). Cast: Maya (primary, running example), Devon (GE/crypto tax credit), Priya (ESG hidden-versions; check-Q green-building). Method deferrals stated: matching/PSM→3.2, IPW/AIPW→3.3, IV/2SLS+LATE→3.4, synthesis→3.5. "Your Turn"→nb3.1 (sim full science table, verify decomposition on the dollar, randomization collapses bias, optional spillover-breaker); 3 check questions.
- [CHECK]: none. No citations requiring page verification (Rubin 1970s, Neyman, Holland 1986 referenced by name/era only — standard attributions, no page cites). NB3.1 is described in detail for CoderAgent (generate both potential outcomes, savviness-driven self-selection, known true effect, fair-coin re-randomization, optional spillover) — no committed code in chapter (prose-only foundation chapter, by design). Numbers in §3.1.5 (0.62/0.55/0.41 → ATT 0.07 + bias 0.14 = 0.21) are illustrative/self-consistent, not script output.
