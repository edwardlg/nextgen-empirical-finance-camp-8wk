# Handoff w6-assess6 — Week 6 Assessment (Reading the Frontier II: build+validate a text classifier)
Wrote book/weeks/week-06/assessment6.md (~4,570 words; student-facing intro+PartA+PartB+rubric, instructor key adds the worked metric computations). CENTERPIECE = Part A (70 pts): student defines a text-classification problem on PUBLIC EDGAR text (3 candidate tasks tied to cast — 8-K event-type / 10-K risk-factor tone / disclosure-topic), hand-labels ≥60 random snippets, seeded stratified dev/held-out-test split (≥20 test, touched once), builds a FROZEN classifier via one of three routes (local TF-IDF/logreg or dictionary baseline = no API key needed, OR LLM via nb6.5 provider switch w/ rubric cached + env-only key, OR Ollama local fallback for licensed text), then an OOS report = labeled confusion matrix + per-class & macro precision/recall/F1 + Cohen's kappa vs human + bootstrap/normal CI on headline metric; bare accuracy forbidden as headline. A.4 = leakage/look-ahead audit (BOTH pipeline-leakage AND training-data/cutoff leakage, task classed contemporaneous-vs-predictive); A.5 = reproducibility/disclosure (pin model version/provider or local lib+seed, determinism caveat — Opus 4.7 has no temperature knob so save labels to disk, JSONL audit log, env-vars-only-no-secrets). Part B (20 pts): B1 text-as-data=measurement-not-causality (tie to a W6 paper's validation table); B2 why raw accuracy misleads under imbalance (worked: acc .92, rare-recall 0, precision undefined); B3 detect/prevent look-ahead leakage (Sam's 2021 10-K "prediction" = memorized outcomes); B4 what to disclose about AI use (label-gen needs validation table / prose-edit doesn't / suggested citations most dangerous → verify or [CHECK]). Part C analytic rubric 5 criteria × 4 levels, 100 pts explicit, WEIGHTED HARD toward validation+honesty+repro: validation-rigor 24 / leakage-&-limitations-honesty 20 / reproducibility-&-disclosure 18 / classifier-build correctness 8 / presentation 10 — top 3 rows = 62 (>3× the build weight); a hard-coded key alone caps the repro row at Missing per CONVENTIONS §5. Instructor key has worked computations: binary P=.80/R=.667/F1=.727/acc=.70, imbalance degenerate case, Cohen's kappa worked (p_o=.80,p_e=.62→κ=.474), macro-vs-micro, model-answer sketches for all 4 B items, plus a "top report in one paragraph" exemplar.
- [CHECK]: none new. Consistent w/ ch65 + reading-guide-pack-6 + nb6.5 handoff (8-K dataset, local logreg+keyword backstop, JSONL log, date-filter leakage demo, prompt caching/cache_read_input_tokens). Used cast (Maya/Devon/Priya/Sam/Leah); model IDs claude-opus-4-7 + GMU Azure AZURE_OPENAI_KEY per ch65; secrets via os.environ ONLY (grep clean — no sk-/Bearer/inline api_key); no emojis; CONVENTIONS voice + §5 repro discipline + §6 [CHECK]-not-fabricate enforced. Total 100 normalized (Part B's 20 folded into the leakage [B1,B3], validation [B2], repro [B4] rows). Local fallback path means students need NO API key to complete the assessment. No companion notebook written (assessment reuses nb6.5; not a separate code deliverable).
