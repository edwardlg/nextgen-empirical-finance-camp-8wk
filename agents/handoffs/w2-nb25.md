# Handoff — nb2.5 Biased-Estimator Lab

- Built `notebooks/week-02/nb2.5-biased-estimator-lab.ipynb` (24 cells, alternating markdown/code): OVB (verifies β̂_short → β₁+β₂·δ₁ ≈ -0.15, bias does not shrink with N, two-sign rule across all four sign combos), attenuation (β̂ → λ·β, matched to 3dp, plotted vs reliability ratio), mismeasured-y (slope unbiased, SE inflates), and functional-form/RESET (linear fit fails RESET F=1125 p≈0; quadratic passes F=0.05 p=0.95). "Your Turn": δ₁=0 orthogonal omitted var → bias vanishes, only precision cost.
- Confirmed statsmodels 0.14.6 signature: `linear_reset(res, power=3, test_type='fitted', use_f=False, cov_type='nonrobust', cov_kwargs=None)` — first positional arg is a fitted RegressionResults; default test_type='fitted' implements Ramsey's procedure (regress on ŷ powers); used power=3, use_f=True explicitly.
- Verified: extracted code ran under python3, exit 0, all in-notebook asserts PASSED; .ipynb validates via nbformat with empty outputs; rng=default_rng(42), matplotlib Agg, no chained indexing, no deprecated pandas. Temp files cleaned.
