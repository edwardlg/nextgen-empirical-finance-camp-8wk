# Handoff w6-nb61 — CoderAgent (nb6.1: Patent-Value Panel, KPSS-style)

Built `notebooks/week-06/nb6.1-patent-value-panel.ipynb`: 25 cells (13 md, 12 code; strictly alternating), nbformat v4 validates, outputs empty, `rng=np.random.default_rng(42)`, `matplotlib.use("Agg")`. Seeded synthetic, fully offline. Pipeline: (1) simulate 60 firms x 4yr daily returns via market model R_it=a+b*R_mt+e + 633 patents w/ grant dates and a right-skewed log-normal TRUE dollar value (top 10% hold 59% of value); (2) inject each patent's value as a 3-day grant-window abnormal return = v_true/mktcap, plus an optional confounding earnings-surprise shock; (3) reconstruct KPSS-style value per patent via a Week-4 market-model event study (120-day est window, 10-day gap, 3-day window CAR x mktcap -> $), aggregate to a 217-row firm-year innovation-value panel; (4) validate — measure predicts a planted next-year profitability (driven by latent firm q_firm); (5) break it — sweep % of contaminated windows and watch recovery + prediction degrade. Non-executed (`RUN_REAL=False`) real-path cell documents USPTO PatentsView bulk API + licensed CRSP-on-GMU-infra (snapshot pin per Conventions §5) + public KPSS dataset (github.com/KPSS2017) — no fabricated real numbers. "Your Turn": vary window length, raise noise floor, make confound systematic.

VERIFIED: code extracted to temp .py, ran under python3 -> EXIT 0; formal assertions all pass — recovery corr level=0.565 / log=0.382 / firm-year=0.722 (aggregation averages out per-patent noise); validation coef=+0.0054 t=3.61 p=0.0004 (true-value benchmark t=9.20); degradation monotone: recovery 0.382->0.253 and validation_t 3.61->2.45 as contamination 0%->75% (averaged over 5 confound seeds for smoothness). No deprecated pandas (only plain-list `.append`), no chained indexing, `.copy()` in every derived-frame helper. Temp .py + builder script cleaned.

Pedagogy notes for downstream: per-patent recovery is deliberately imperfect (~0.4-0.57) — realistic signal-to-noise (median patent ~0.4% window blip vs ~2-4% 3-day idiosyncratic noise), which is exactly WHY firm-year aggregation (corr 0.72) and the short window are the defenses; this is a feature, taught explicitly in §6.1 and the Your-Turn check questions. Measure stays significant even at 75% RANDOM contamination because idiosyncratic confounds wash out under aggregation — the Your-Turn extension #3 (make the confound systematic / correlated with v_true) is the dangerous case that does NOT wash out, per Reader's-Guide §6. Wires Week-4 event study (market model + CAR + estimation-window hygiene/gap) and Week-1 measurement (construct-vs-measure gap, right-skew motivation, selection: patents w/o pre-history get NO value). Note: nb6.2-6.5 already present in week-06/ (sibling agents).
