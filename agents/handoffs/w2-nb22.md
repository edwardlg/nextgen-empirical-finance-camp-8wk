- nb2.2 Gauss–Markov Monte Carlo built (25 cells, alternating markdown/code): verifies unbiasedness (mean β̂ → true [12,-0.42]), variance formula σ²(X'X)⁻¹ (emp/theory ratio ≈ 0.999), efficiency vs a half-sample rival (OLS 2.3x tighter = BLUE), and breaks homoskedasticity (WLS 1.49x tighter, classical SE off ~44%) to tee up Ch 2.4.
- "Your Turn" cell lets students set gamma≠0 to violate E[ε|X]=0 and watch bias appear / get precisely-wrong with larger N, foreshadowing Ch 2.5; uses rng=default_rng(42), matplotlib Agg, np.ptp (NumPy-2 safe), .copy() on subsample slice.
- Verified: extracted code ran via python3, exited 0, all asserts passed; .ipynb validates with nbformat and has empty outputs; temp files cleaned.
