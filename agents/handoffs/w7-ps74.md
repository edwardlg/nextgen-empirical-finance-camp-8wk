# Handoff w7-ps74 — WriterAgent (PS 7.4 + model deliverable)

PS 7.4 (`book/weeks/week-07/ps7.4.md`, ~3,350w) — build-and-document project-scaffolding sheet for Ch 7.4/nb7.4: 6 problems × 100 pts (P1 merge contract + crosswalk + validate=/match-rate 18; P2 survivorship per-year churn + dlret 16; P3 look-ahead available_date + merge_asof(backward) + no-look-ahead assert, incl. forward fires-demo 20; P4 winsorize-by-date + do-not case 14; P5 missing-data MCAR/MAR/MNAR probe + logged listwise 16; P6 raw→intermediate→analysis step() audit trail + summary-stats 16), per-cast project (Sam/Maya/Priya/Devon/Leah), graded submission checklist, all diagnostics labeled illustrative; §5 hygiene enforced (validate= every merge, .copy(), no df.append, no chained-index, pinned snapshot, env secrets). Model deliverable (`book/appendices/E-solutions-manual/E-w7-ps7.4-solutions.md`, ~4,730w) — one fully worked exemplar (Sam's value panel) at A-grade: merge-contract table, 97.6% match rate, churning per-year counts, +0.30% survivorship gap, 6-mo FF lag worked example (fy2017→2018-06-30), merge_asof code + assert that passes (backward) and fires (forward, 2108/2556 rows), by-date winsorize (max 440→280, N preserved), MCAR-ish listwise log, full row-count audit trail w/ per-gap defense, summary-stats table w/ sanity checks, plus instructor grading notes (per-problem rubrics, 5 common failure modes + deductions, cross-project mapping, [CHECK] LINKTYPE + illustrative-vs-real labeling guidance). All numbers illustrative (NOT real CRSP/Compustat); licensed-data §5 rule restated.

VERIFIED: extracted every load-bearing snippet (validate=many_to_one + indicator match rate; available_date=June30(y+1); merge_asof backward assert HOLDS + forward assert FIRES; winsorize-by-date preserves N; key-uniqueness; pd.concat-over-list) to /tmp and ran under python3 -W error::FutureWarning -W error::DeprecationWarning → EXIT 0, all asserts pass, forward-demo confirmed to fire. Consistent w/ nb7.4 handoff (97.6% match, secondary share class 90001 unmatched, +0.30% survivorship, MCAR-by-construction synthetic, "Your Turn" #1 MNAR / #2 forward-fires). Scratch file cleaned.
