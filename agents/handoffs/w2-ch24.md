# Handoff w2-ch24 — WriterAgent (Ch 2.4: Heteroskedasticity, Clustering, HC/HAC)

- Word count: ~6,280 (`wc -w` = 6,282). File: `book/weeks/week-02/ch24-heteroskedasticity-clustering-hac.md`. Started from Ch 2.2's classical variance $\sigma^2(\mathbf{X}'\mathbf{X})^{-1}$ and the assumption $\operatorname{Var}(\boldsymbol\varepsilon\mid\mathbf{X})=\sigma^2\mathbf{I}$; built up the sandwich $(\mathbf{X}'\mathbf{X})^{-1}\mathbf{X}'\hat{\boldsymbol\Omega}\mathbf{X}(\mathbf{X}'\mathbf{X})^{-1}$ as the unifying object. Theme is SEs not bias ("point estimate fine, t-stat lies"); explicitly defers OVB/bias to Ch 2.5. Code block VERIFIED to run on statsmodels 0.14.6 — produces beta≈0.27 (true 0.30) glued across all SE flavors while firm-clustered SE roughly doubles classical (0.073→0.144, t 3.7→1.9). The §2.1 by-hand sandwich example (x=1,2,3,4) verified: World B var=354/900≈0.393 vs classical 1/30≈0.033, a 12x gap / 3.5x SE understatement. NOTE: had to fix the original `se()` helper — robust results from `get_robustcov_results` return plain numpy arrays (not pandas Series), so the snippet indexes the `x` coefficient by position (`xi`), not by name; downstream notebook nb2.4 should use the same pattern.

- Terms DEFINED here (later chapters reference back, don't redefine): error variance-covariance matrix $\boldsymbol\Omega$; homoskedasticity vs heteroskedasticity; sandwich/robust variance estimator; White (1980) HC estimator; HC0/HC1/HC2/HC3 and the leverage $h_{ii}$ (hat matrix from Ch 2.1) finite-sample corrections; Breusch–Pagan & White tests (mentioned, "don't bother testing, just use robust"); within-cluster correlation; cluster-robust sandwich (block-diagonal $\boldsymbol\Omega$); Moulton-style inflation factor $1+(n-1)\rho_x\rho_\varepsilon$; few-clusters caveat (aim ≥30–50 G) + wild cluster bootstrap; serial/autocorrelation; HAC / Newey–West (1987), Bartlett kernel, bandwidth $L$; effective sample size intuition; Petersen (2009) taxonomy (firm effect→cluster by firm; time effect→time FE or cluster by time; both→two-way); FE-vs-clustering distinction (NOT substitutes). Citations used: **Petersen (2009) RFS 22(1):435–480** (full, as instructed); White (1980) and Newey & West (1987) referenced by name/year only (no page cites — concepts standard).

- [CHECK] items: (1) Cameron, Gelbach & Miller 2008 (wild cluster bootstrap) and 2011 (two-way clustering) referenced by author/year — tagged `[CHECK pages]` in text; NOT in the locked citation list, MentorAgent/editor should verify or supply full bib. (2) Numbers in the two illustrative tables (Priya HC0–HC3 SEs in §3.4; Maya panel SE-flavor table in §6) are plausible *illustrative* values, NOT outputs of a committed script — flagged as "roughly"/"in the spirit of Petersen's tables"; CoderAgent building nb2.4 should either reproduce comparable magnitudes or the editor should soften to clearly-hypothetical. The §7 code block and §2.1 hand example ARE verified. (3) Forward-refs to honor: Ch 2.3 (FWL/demeaning = fixed effects), Ch 2.5 (OVB/bias — explicitly the "other job"), Ch 5.4 (full Petersen reader's guide — I deliberately previewed only the operational rule to avoid overlap), Weeks 3–4 (panel/FE methods, FE-vs-clustering developed there). "Your Turn" points to nb2.4 (SE flavors on clustered panel, incl. shrink-G experiment) — CoderAgent owns it; §7 snippet is the seed.
