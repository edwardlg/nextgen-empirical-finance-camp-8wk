# w4-nb44 — Synthetic Control & Synthetic DiD (CoderAgent)

Built `notebooks/week-04/nb4.4-synthetic-control-sdid.ipynb` (26 cells, alternating md/code). Both a **hand-rolled scipy SLSQP convex solve** and **pysyncon 1.5.1** (`Dataprep` + `Synth`, premium-per-pre-year special predictors, Nelder-Mead) fit the synthetic control and agree to rounding — they sit side by side so students see the mechanics and trust the package; pysyncon's `att()`/`weights()` used directly, hand-rolled solve drives the placebo loop + plots.
Reproduces the chapter exactly: pre-RMSPE 0.035, post-RMSPE 1.52, ratio 43.5 (~44), effect 1.51 (true 1.4), placebo rank 1/31 p=0.032; no-effect check rank 5/31 p=0.16; SDID (own unit+time-weight impl with Arkhangelsky ridge + placebo SE) tau=1.40, plain DiD 1.43 — all bracket truth, SDID closest with a usable CI.
Verified: extracted code ran `python3` exit 0; nbformat valid, outputs empty; no chained indexing / deprecated pandas; `rng=default_rng(20260528)`, `matplotlib.use("Agg")`. Temp files cleaned.
