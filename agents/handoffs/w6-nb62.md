# Handoff w6-nb62 — CoderAgent (nb6.2: 10-K text vectorization & similarity, Hoberg–Phillips TNIC-style)

Built `notebooks/week-06/nb6.2-10k-text-similarity.ipynb`: 30 cells (15 markdown book-voice + 15 code, strict alternation), valid nbformat v4 (validate PASS), outputs empty + execution_count None, `rng=np.random.default_rng(42)`, `matplotlib.use("Agg")`. SKLEARN throughout: `CountVectorizer`(bag-of-words) + `TfidfVectorizer`(stop_words="english", default L2-norm + smoothed idf) for vectors, `cosine_similarity` for the full pairwise matrix. Self-contained offline corpus = 16 synthetic "10-K Item 1" paragraphs written in-notebook across 3 latent industries (5 software/cloud, 5 bank/lending, 4 retail/apparel) + 2 deliberate cross-over firms (OmniMart = retail+cloud / Amazon-like; FinShop = BNPL bank+retail). Flow: (1) corpus + (2) hand-coded "SIC-like" one-box classification as foil (OmniMart→Retail, FinShop→Banking, each losing half its identity) → (3) bag-of-words → (4) TF–IDF w/ idf=log(D/d_j) shown + idf-weight inspection → (5) cosine matrix → (6) cluster-ordered heatmap (3 diagonal blocks) → (7) within-vs-cross headline check → (8–10) TNIC firm-specific industry via threshold τ, firm-specific + intransitive proofs, τ-sweep plot → (11) TNIC-vs-SIC contrast on cross-overs → (12–13) Ch 1.2 (correlation=cosine) + Ch 6.5 (embeddings) bridge w/ negation-ceiling demo + NON-EXECUTED real-path cell (edgartools/requests EDGAR Item 1 pull w/ SEC user-agent + Hoberg–Phillips Data Library pointer [CHECK URL]) → "Your Turn" (add firm / change threshold / bigrams / drop stopwords) w/ bigram scaffold.

VERIFIED: extracted executable cells (skipping the non-exec REAL-PATH cell) to temp .py, py_compile clean, ran under python3, EXIT 0. Headline claims hold: TF-IDF within-cluster cosine 0.402 >> cross-cluster 0.056 (separation ratio 7.19); TF-IDF lowers spurious cross-cluster contamination vs raw counts (0.056 < 0.082) and raises the within/cross ratio (7.19 > 6.69) — asserted on the ratio NOT the raw difference (counts inflate all magnitudes, so the raw gap is misleadingly larger for counts; this was a real bug caught in verification and the framing/asserts were corrected). TNIC behaves as described: τ=0.15 gives firm-specific neighbor sets (SoftA≠SoftB), a concrete intransitive triple SoftA–OmniMart–RetA (0.444, 0.350 ≥ τ but sim(SoftA,RetA)=0.000 < τ), industry size monotonically non-increasing in τ (asserted), and both cross-over firms span ≥2 SIC-like industries under TNIC while the box shows one. No chained DataFrame indexing, no pd.append, no deprecated pandas. Fully synthetic, no licensed/network data. Temp build/verify scripts + 2 runtime PNGs cleaned; only the .ipynb remains.

Notes for reviewer/Mentor: (a) the within-vs-cross comparison deliberately reports the SEPARATION RATIO and the cross-cluster level rather than the raw within−cross difference — flag if the chapter prose implies the raw gap should grow under IDF (it does not on this small corpus). (b) Real-path cell is intentionally unexecuted (network + SEC user-agent + edgartools needed); Hoberg–Phillips Data Library URL left as [CHECK] consistent with the ch62 guide's open [CHECK]. (c) negation demo prints cosine≈0.847 for opposite semiconductor claims to motivate Ch 6.5 embeddings; uses default stop-word drop of "not" as the teaching point.
