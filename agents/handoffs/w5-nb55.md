# Handoff w5-nb55 — CoderAgent (nb5.5: BDM placebo-law simulation)

Built `notebooks/week-05/nb5.5-bdm-placebo-law.ipynb`: 24 cells (14 markdown book-voice + 10 code, alternating), outputs empty, valid nbformat v4, `rng=np.random.default_rng(42)`, `matplotlib.use("Agg")`, statsmodels `smf.ols` only. DGP: 50 states x 20 years, Y_st = mu_s + lambda_t + AR(1) u_st (innovation var scaled to stationary so rho only changes correlation, not noise level), NO treatment term (true effect=0 by construction). Placebo law = random 50% of states switch on at a random per-state onset year. Sections: (1) build panel + viz (drifting state paths + within-state lag-1 ACF hist rho=0 vs 0.8); (2) one placebo TWFE DiD `Y~D+C(state)+C(year)` w/ classical SE; (3) MC headline 600 reps -> naive rejection rate; (4) fixes = cluster-by-state + collapse-to-pre/post; bar chart by method; (5) rho sweep {0,.2,.4,.6,.8,.9} x 300 reps, naive-vs-cluster line plot. Collapse fix = proper 2x2 DiD (split at median treated-onset year, keep BOTH treated+control states so year shocks difference out, read `ever_treated:post` interaction) — the obvious treated-only collapse leaves year-shock confound and only reaches ~0.17, so I kept controls. Connects to Week 1 (size/Type I/alpha, coin-flip sim), Ch 2.4 (cluster-robust SE, block-diagonal Omega), Week 4 (TWFE DiD spec verbatim, cluster-by-treatment-unit, few-clusters caveat). "Your Turn": 4 extensions (sweep T; shrink n_states to rediscover few-clusters over-rejection; add block bootstrap; put a real effect back to compare power).

VERIFIED: code extracted to temp .py, `py_compile` clean, run under python3, EXIT 0 (~6.5 min wall, dominated by the 600+1800 statsmodels fits — well within budget for a one-time run; loop is modest as required). REPRODUCES BDM at rho=0.8/600 reps: naive=0.350 (7x nominal 5%), cluster=0.050 (exactly honest), collapse=0.055 (near honest). Rho sweep: naive 0.037(rho=0)->0.113->0.133->0.223->0.343->0.347(rho=0.9), cluster flat 0.04-0.06 throughout — clean mechanism evidence that serial correlation (not chance) is the culprit and clustering defuses it. Temp build/verify scripts, __pycache__, and 3 runtime PNGs (serial-correlation, rejection-by-method, rho-sweep) all cleaned; only the .ipynb remains.

Notes for reviewer/Mentor: (a) Filename is `nb5.5-bdm-placebo-law.ipynb` per the task path, NOT the ch5.5-prose-inferred `...-simulation.ipynb`; chapter §References-out pointer says `nb5.5-bdm-placebo-law-simulation.ipynb` — flag if the prose link should be updated to match, or rename the nb. (b) No precise BDM table figure is quoted (honoring the chapter's [CHECK] on the ~0.45 headline); our 0.35 is presented as "this DGP's" rate, explicitly framed as illustrative-of-mechanism not a universal constant, consistent with chapter §6. (c) Collapse uses a single common split year (median onset) rather than per-state event-time alignment — pedagogically simplest faithful 2-period DiD; a reviewer may want a sentence noting heterogeneous-onset collapse is subtler. (d) Block bootstrap is left as a "Your Turn" exercise (task said cluster AND/OR collapse for the in-notebook fixes; both are implemented). (e) Fully synthetic, no licensed data.
