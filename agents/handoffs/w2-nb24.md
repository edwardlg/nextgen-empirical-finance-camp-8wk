# Handoff w2-nb24 — CoderAgent (nb2.4: SE flavors on a clustered panel)

Built `notebooks/week-02/nb2.4-se-flavors-clustered-panel.ipynb`: 26 cells (14 markdown, 12 code), outputs empty, valid nbformat v4. Simulates a 120-firm x 20-year panel (N=2,400) with a persistent firm effect, a milder common year effect, and firm-level heteroskedasticity exp(0.9|x_firm|); error vector residualized on [1,x] so beta-hat lands EXACTLY on the true 0.30 across every SE flavor (the "estimate never moves" point is then unmistakable). Reproduces the chapter's collapse: classical SE 0.0526 (t=5.70) -> White HC0-HC3 ~0.0715 (t~4.2) -> Newey-West HAC(4) 0.0863 (t=3.47) -> firm-cluster 0.1372 (t=2.19) -> two-way firm+year 0.1292 (t=2.32). That is the chapter's 6.0 -> ~2.2.

Clustering API: primary path is statsmodels `fit.get_robustcov_results(cov_type=..., groups=...)` for ALL seven flavors (HC0/HC1/HC2/HC3 via "HC*"; one-way via `cov_type="cluster", groups=df["firm"]`; two-way via `groups=df[["firm","year"]]`; HAC via `cov_type="HAC", maxlags=4`). Cross-checked the firm cluster against linearmodels `PooledOLS(...).fit(cov_type="clustered", cluster_entity=True)` on a (firm,year) MultiIndex — agrees to 0.0006 (df adjustment). Honored the handoff warning: robust results return numpy arrays not pandas Series, so the `se_row` helper indexes the x coefficient by integer position `xi`, never by name. Includes residual-fan plot, SE-by-method bar chart, a few-clusters experiment (G shrunk 120->4, SE visibly wanders below ~30), and a "Your Turn" `simulate_panel(firm_shock_strength=...)` sweep where cluster/classical SE ratio rises monotonically 1.60 -> 3.64.

VERIFIED: code extracted to temp .py, run under python3 with matplotlib Agg, exits 0 with no errors (only benign Agg "non-interactive cannot be shown" warnings); asserted clustered SE > classical SE and two-way SE > classical SE both pass. The §2 four-point by-hand sandwich matches the chapter (World B var 0.393 vs classical 0.033, 3.4x SE understatement). Temp files cleaned. Note: chapter §6's illustrative Maya table (classical t=5.0->two-way t=1.7) was [CHECK]-flagged as hypothetical by WriterAgent; this notebook supplies real, reproducible magnitudes of comparable spirit (t 5.70->2.19), so the editor can point the prose at nb2.4 rather than soften.
