# Handoff w3-ch33 — WriterAgent (Ch 3.3: Entropy Balancing & Doubly-Robust Estimation)

- Word count: ~6,330 (`wc -w` = 6,331). File: `book/weeks/week-03/ch33-entropy-balancing-doubly-robust.md`. Builds directly on Ch 3.2's propensity score $e(\mathbf{X})=\Pr(D=1\mid\mathbf{X})$, CIA, and overlap (explicitly does NOT re-derive them); explicitly forward-refs Ch 3.4 (IV as the escape when CIA fails / unobserved confounder). Anchored on Maya (financial-literacy enrollment → loan rate, reweight non-enrollees to look like enrollees) + Priya climate-insurance variant (drought insurance → irrigation). Theme thread = STABILITY (all consistent in theory; differ in how they misbehave under near-violated overlap). VERIFIED: §4 code block runs end-to-end on numpy/pandas/statsmodels — naive diff -1.50 (biased), IPW(Hájek) -0.785, AIPW -0.779 vs true -0.80; max weight 10.0, eff. N 2779/4000. §1.4 by-hand IPW example also verified (treated Hájek mean 8.45, control 9.23, est -0.78; naive -1.0). Reveal-the-trick structure throughout (plain sentence → intuition → numerical example → algebra → failure mode → code).

- Terms DEFINED here (later chapters reference back, don't redefine): inverse-probability weighting (IPW); pseudo-population; the LIE-based proof that dividing by $e(\mathbf{X})$ recovers $\mathbb{E}[Y(1)]$ (propensity cancels under CIA); Horvitz–Thompson vs Hájek (self-normalizing) estimators; IPW explosion / extreme-weight problem; trimming, stabilized weights ($\hat p/\hat e$), effective sample size $(\sum w)^2/\sum w^2$; entropy balancing (Hainmueller 2012) — direct moment-matching constraints + max-entropy/KL-to-uniform objective, closed form $w_i\propto q_i\exp(-\sum\lambda_k X_{ik})$, exact-balance-by-construction vs PSM/IPW approximate balance; regression imputation / g-computation ($\hat\mu_1,\hat\mu_0$); AIPW / doubly-robust (Robins–Rotnitzky–Zhao 1994) — augmented = outcome model + IPW-corrected residuals; the two-case "doubly" argument; Neyman orthogonality → double/debiased ML (Chernozhukov et al. 2018); §5 decision guide PSM vs entropy balancing vs AIPW (report >1). Estimands ATT/ATE referenced from Ch 3.1.

- [CHECK] items: NONE flagged in text — all three required citations rendered verbatim with full bibliographic info and high confidence: Hainmueller (2012) Political Analysis 20(1):25–46 (locked list, full title); Robins, Rotnitzky & Zhao (1994) JASA 89(427):846–866 (instructed by name/year — added full cite, standard); Chernozhukov et al. (2018) Econometrics Journal 21(1):C1–C68 (added to anchor the DML connection; widely-cited standard, but EDITOR may verify the C-prefixed page range if desired). "Your Turn" → nb3.3 (IPW vs entropy balancing vs AIPW, incl. break-one-model-at-a-time double-robustness demo + entropy-weight tameness vs IPW explosion) — CoderAgent owns nb3.3; §4 snippet is the seed (note: uses `.clip(0.01,0.99)` for trimming and stabilized Hájek weights; entropy-balancing solve deferred to the notebook via `ebal`/short convex solver). 3 check questions at end (extreme-weight nervousness; AIPW survives wrong outcome model if propensity right; unobserved-confounder ceiling → Ch 3.4 IV).
