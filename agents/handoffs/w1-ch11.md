# Handoff w1-ch11 — WriterAgent (Ch 1.1)

- Word count: ~5,100 words (`wc -w` = 5,106; includes one runnable Python code block and LaTeX math, so prose is right at the ~5,200 target). File: `book/weeks/week-01/ch11-joint-conditional-two-laws.md`. Verified the LTV code snippet runs and reproduces the cited numbers (E[X]=0.300, within=1.710, across=0.400, Var(X)=2.110).
- Terms DEFINED here (later chapters should NOT redefine — reference back to Ch 1.1): random variable; joint distribution / joint probability mass function; marginal distribution (marginalization); conditional distribution / conditional pmf; multiplication rule; independence (and the "uncorrelated ≠ independent" warning via the Y/Y² example); expectation/mean; conditional expectation E[X|Y=y] AND the random-variable view E[X|Y]; variance (incl. E[X²]−(E[X])² form); Law of Iterated Expectations (LIE / tower property); Law of Total Variance (LTV, within- vs across-group decomposition); probability density function + continuous conditioning (briefly, §6); reverse conditioning / Bayes-in-table-form (introduced, NOT formalized — flagged for whoever owns a Bayes treatment). NOTE: covariance and correlation are mentioned but deliberately LEFT for Ch 1.2 to formalize (kept algebra light per brief); only an intuitive "linear association" gloss given.
- [CHECK] items: (1) No citations made in this chapter, so nothing to verify bibliographically. The Sam momentum framing is thematically consistent with the Intraday Momentum paper (Gao, Han, Li & Zhou 2018) cited in Mentor Session 1, but I made NO empirical/numerical claim attributed to it — all numbers are from an invented illustrative joint table. (2) Forward-references made that must stay accurate: Ch 1.2 (covariance/correlation as geometry — cosine/inner-product framing), Ch 1.3 (sampling distributions; nb1.1 convergence bridges to it), Ch 1.4 (heavy-tailed/crypto-Devon tails breaking LLN, used as the "when LIE/LTV fail" failure mode). If any of those scopes shift, update the cross-refs in §3, §5, §6, §7, §8 and the nb1.1 pointer. (3) Notebook nb1.1 ("LIE/LTV by simulation") is pointed to from "Your Turn" and the in-text code note but not yet built — CoderAgent owns it; the chapter's code block is a clean starting seed.
