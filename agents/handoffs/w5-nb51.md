# Handoff w5-nb51 — CoderAgent (nb5.1: FF92 portfolio sorts)

Built `notebooks/week-05/nb5.1-ff92-portfolio-sorts.ipynb`: 31 cells (17 md, 14 code; alternating), outputs empty, nbformat v4.5 validates. Path A = illustrative-only WRDS pull (`RUN_PATH_A=False`) showing real CRSP msf returns + mktcap (abs(prc)*shrout) and Compustat funda book-equity schema with pinned snapshot `2025-12-31` and the July-to-June lag note (no fabricated CRSP numbers); Path B = seeded synthetic 1500-firm x 240-month panel (`rng=np.random.default_rng(42)`, `matplotlib.use("Agg")`) with planted monthly premia LAM_SIZE=0.0025, LAM_VALUE=0.0035, MKT_MEAN=0 so beta is priced into nothing; beta correlated with size (-0.77) to stage the §5 tangle.
Covers all three Ch5.1 §7 exercises: (1) univariate size & BE/ME decile sorts — recovers small>big (+1.21%/mo) and value>growth (+1.51%/mo), plus a look-ahead "break it on purpose" demo (contaminated sort inflates +3.58%/mo); (2) 5x5 double sort — both premia positive in EVERY bucket of the other; (3) Fama-MacBeth (Lab-2 machine verbatim, SE=sd(slopes)/sqrt(T)) with portfolio pre-formation betas (corr 0.976 with true beta, defeats the EIV/attenuation excuse) across 3 specs — beta t collapses +1.87(alone)->-0.21(kitchen sink) while log_size t=-16.3 and log_bm t=+33.6 survive.
VERIFIED: code extracted to temp .py, ran under python3, EXIT=0; assertions confirm size premium (small>big), value premium (value>growth), value/size spreads positive in all 25 cells, and beta insignificant + size/BE/ME significant in the kitchen sink. No deprecated pandas (only a plain list `.append` in the FM loop), no chained indexing, `.copy()` used in helpers. Temp .py, figures, and builder cleaned. Note: beta-alone t=1.87 (not ~0) is a feature — omitting size lets the size premium leak into beta via the -0.77 corr, a live demo of why the double sort/horse race is needed; the planted beta premium is zero and the kitchen-sink slope confirms it.
