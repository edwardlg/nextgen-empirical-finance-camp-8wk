# Handoff — WriterAgent → Reviewer: Ch 2.2 Gauss–Markov and the Meaning of "Best"
Word count: 5,107 (incl. one Python block ~35 lines + a hand worked Var(beta_1) example; prose ~4,500; target was ~6,000 but matches the established technical-chapter band of Wk1, e.g. Ch1.3=4,336, Ch1.5=5,519). Cast: Maya (loan APR ~ income, fair-lending angle), continuous throughout.
Terms defined on first use: the five classical assumptions stated as testable claims (linearity in parameters, random sampling / no perfect collinearity = full-rank X, zero conditional mean E[eps|X]=0, homoskedasticity Var(eps|X)=sigma^2 I, normality add-on); contamination identity beta_hat = beta + (X'X)^{-1}X'eps; unbiasedness of OLS (proved via E[eps|X]=0); variance formula sigma^2 (X'X)^{-1} + scalar Var(beta_1)=sigma^2/sum(x-xbar)^2; BLUE (Best Linear Unbiased Estimator) decoded letter-by-letter; Gauss–Markov theorem + sketch (rival = OLS + Dy, DX=0, Var = OLS + sigma^2 DD'); efficiency (relative to class; dies w/ heteroskedasticity; normality promotes to all-unbiased); multicollinearity (named, deferred to 2.3); unbiased vs consistent (independent properties; OLS has both; "consistent estimator of wrong target"); weighted least squares (named). Forward-points: 2.3 (functional form/multicollinearity), 2.4 (heteroskedasticity + robust HC1/HC2/HC3 + clustered SEs), 2.5 (OVB + measurement error, off the proof-collapse line). Builds on Ch2.1 (beta_hat formula, H/M, full rank — not re-derived) and Ch1.3 (bias/var/MSE/consistency/plim) + Ch1.4 (CLT) + Ch1.5 (t-tests) as referenced.
[CHECK]: none — no citations made (Gauss–Markov is common knowledge, stated without bibliographic claim). Notes for reviewer: (1) numeric examples in §3 (se≈0.0025) and §6 (the -0.42 OVB scenario) are illustrative, stated as such; the §4 code block is the truth-check skeleton that nb2.2 extends. (2) Ch2.1 not yet written as a file; cited by content per its brief-stated notation. (3) Full BLUE proof deliberately left as sketch (brief said "sketch of why"); flagged in-text as an appendix exercise.
