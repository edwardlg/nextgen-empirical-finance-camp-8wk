nb7.5 — First-Look Regressions (Frozen Until the PAP Is Filed): built at notebooks/week-07/nb7.5-first-look-regressions.ipynb (21 cells, alternating book-voice markdown + code; valid via nbformat, outputs empty). Synthetic county-year fair-lending DiD panel (rng=default_rng(42), matplotlib Agg), planted ATT = -1.80 pp; a prominent PAP-FILED GATE (markdown + `PAP_FILED=True` flag) is asserted before the analysis cell runs, embodying "frozen until filed".
Pre-registered primary spec run ONCE via pyfixest `feols("gap ~ post_treat + income + ltv | county + year", vcov={"CRV1":"state"})` recovers the planted effect: beta=-1.886, clustered-by-state SE=0.065, 95% CI [-2.019, -1.752] covers -1.80. A fenced "EXPLORATORY (not confirmatory)" section shows the garden-of-forking-paths (alt clustering/controls/sample/over-control) and a §6 demo proving the gate assert fires when PAP_FILED=False; §7 bridges to Week-8 robustness on the frozen result.
VERIFIED: extracted code to temp .py, ran python3 -> exit 0; all internal asserts pass (CI covers truth, sign negative, |beta-tau|<0.30, gate fires). Temp files cleaned. [CHECK] none.
