# Handoff: nb7.3-pap-companion-power-calc.ipynb (CoderAgent)

- Built `notebooks/week-07/nb7.3-pap-companion-power-calc.ipynb` = 22 cells (12 markdown + 10 code), alternating reveal-the-trick book voice with runnable code on Maya's HMDA fair-lending design; reuses the nb1.5 power logic (ncp = effect/se, power = tail beyond z_crit - ncp). Delivers all 5 required pieces: (1) ANALYTIC power calculator + its inversion to N-for-80%-power, for a two-group mean comparison AND for a regression coefficient (uses the RESIDUALIZED regressor SD = raw SD * sqrt(1-R^2) to capture variance inflation, not the raw SD); (2) MONTE CARLO power simulator for the actual planned LPM spec (denied ~ const + minority, OLS by hand, classical t-test) with a size check under beta=0; (3) MDE calc (two-group + regression) at the realistic N; (4) runnable BENJAMINI-HOCHBERG FDR routine returning input-order reject-mask + adjusted q-values, plus an all-null calibration check; (5) a PAP-TEMPLATE emitter that prints the filled §7.3.4 skeleton (hypotheses, 7-slot primary spec, MT families, falsification, robustness, hold-out, and a power-summary block) ready to paste into PAP.md and tag `pap-filed`. Explicit callbacks to Wk1 power/size and the §1.5.10 multiple-testing arithmetic (1-0.95^20).
- Verified: extracted all 10 code cells to /tmp and ran with python3 -> EXIT 0, every assertion passed. analytic ~ simulated power confirmed (MC 0.8090 vs analytic 0.8100, diff 0.0010); N-for-80%-power inverts correctly (N_needed=7773 -> power back-check 0.8000, asserted); BH selects the right family (keeps the two smallest p=0.008 & 0.012 where Bonferroni keeps only 0.008, asserted np.array_equal); MC size 0.0487 ~ 0.05; BH realized FDR 0.0420 <= q=0.05; MDE 1.971pp feeds back to power 0.8000. nbformat validates; all outputs empty; rng=np.random.default_rng(42); matplotlib Agg; no chained indexing / no pd.append / no inplace / no deprecated pandas (scanned). Temp .py and the _build_nb73.py builder removed.
- No [CHECK] items; no licensed data touched (illustrative LPM simulation only). All quantitative content is pedagogical (Maya's assumed exploration-set numbers: p_bar=0.12, minority_share=0.30, N=8000, 2pp MDE target) and self-consistent via the back-checks above. Matches the WriterAgent forward-dependency interface in w7-ch73.md: ingests exploration-set residual SD + effective N -> N for 80% power at alpha=0.05, runnable BH, and the pap-filed-template emitter; 30/70 hold-out on hash(loan_id) is named/pinned in the emitted template per CONVENTIONS §5.
