# Handoff w7-nb71 — CoderAgent (nb7.1: Idea-to-Spec Template)

- Built `notebooks/week-07/nb7.1-idea-to-spec-template.ipynb` (23 cells: 15 markdown / 8 code, alternating; created the week-07 notebooks dir). Mostly-structural fill-in template walking puzzle → refined question (4-slot grammar) → directional hypothesis with H0 → identifying variation (Wk3-4 catalog run backwards) → CONVENTIONS §4 spec line; plus the runnable Ch 7.1.7 rubric (`Idea` dataclass, 5 dims ×0-2 =/10, `rank_ideas`), a `spec_line` §4 formatter, a worked filled-in example (Priya disaster DiD, also shows Devon depeg spec), an Agg stacked-bar plot, and a blank student template cell. `rng=np.random.default_rng(42)`, `matplotlib.use("Agg")`, `.copy()` on the ranking DataFrame, no deprecated pandas, no chained indexing.
- VERIFIED: extracted code → temp .py → `python3` exits 0 (only the benign non-interactive Agg `plt.show()` warning). Scoring ranks sensibly: Devon depeg 10/10 → #1 KEEP; Maya HMDA 9 → #2 KEEP; "Bloomberg whisper numbers" total 6 but **VETOED** (feasible=0) → sunk to last. Confirmed edge cases: veto fires on Identifiable/Feasible=0; a vetoed total-8 idea correctly sinks below a clean total-5 idea (rank, not raw sum, wins); `spec_line` raises ValueError naming any blank field; `Idea` rejects scores outside 0-2. In-notebook `assert`s guard the veto behavior. nbformat-valid, all outputs empty / exec_count None. Temp build+extracted files cleaned.
- For downstream: PS 7.1 reuses this exact rubric/veto logic — the `Idea`/`rank_ideas`/`spec_line` API is the shared contract. Datasets named (HMDA, Etherscan+CoinGecko, NOAA/FEMA/FRED, Bloomberg-as-the-infeasible-foil) match Ch 7.1.6; Bloomberg whisper feed used deliberately as the veto example. No new data-cards created (Ch 7.2's job).
