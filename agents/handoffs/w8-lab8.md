# Handoff w8-lab8 — CoderAgent + lab author (Lab 8: "Final Manuscript + Repo + Defense")

- Word count: 4,806 prose words (6,184 incl. code/Make/LaTeX skeletons; target ~4,600, on-target). File: book/weeks/week-08/lab8-final-manuscript-repo-defense.md — terminal lab, 8 of 8. Learning goals + Setup (latexmk/tectonic/pdflatex/make probes; TinyTeX on container) + Part 1 compile LaTeX (finished paper/ layout; minimal-complete AEA-style main.tex skeleton w/ title/abstract/intro/data/design/results+`\input{tables}`+`\includegraphics{figures}`/conclusion/thebibliography; Overleaf 3-move workflow; local latexmk/tectonic path; AEA-class drop-in note) + Part 2 one-click packet (src/config.py one named SEED=20260815 via np.random.default_rng; 01_pull/02_build/03_analysis run-order scripts; Makefile w/ data/tables/figures/paper/all/clean targets + TAB-indent warning; equivalent run_all.py w/ shutil.which compile gate + --clean; `make clean && make all` honesty test w/ md5sum -c; commit-not-upload; no secrets/licensed data) + Part 3 timed talk (3-pass rehearsal protocol + python per-slide timer; target 7 not 8 min; pre-decided cut line; threats-table->4-part-answer prep table; FATAL[attacks comparison/arguable]-vs-SURVIVABLE[attacks number/testable] triage + concede-and-retreat; located "I don't know") + submission checklist (paper PDF / repo URL / deck / packet verified) + Assessment 8 = the capstone (same rubric as Assess 7, execution half) + capstone gallery read-for-seams. Cast: Maya (HMDA fair-lending) throughout. Cross-refs Ch 8.5, Lab 7, Ch 7.5 threats table, Ch 8.2/8.3/8.4, Ch 7.3 pap-filed, Appendix D, CONVENTIONS §4-5.

- VERIFIED on container (python3.12, numpy2.4/pandas2.2/matplotlib3.10/pyarrow23, latexmk+tectonic+pdflatex+make all present): built the full scratch project and RAN it end-to-end. All 5 python skeletons re-extracted from the final doc byte-compile clean (py_compile OK). Makefile dry-run (make -n) shows correct dependency ordering; `make all` ran pull->build->analysis->latexmk and produced a 2-page main.pdf. `make clean && make all` honesty test: table main_results.tex md5 BYTE-IDENTICAL across rebuilds (SEED determinism confirmed); figure PDF differed ONLY in embedded /CreationDate timestamp — diagnosed and fixed in the skeleton via savefig(metadata={"CreationDate": None}), verified data-deterministic after fix. Caught + fixed a REAL skeleton bug during verification: natbib + manual thebibliography throws "Package natbib Error: Bibliography not compatible with author-year citations" — dropped natbib, used plain \cite{}/\bibitem{}; clean latexmk exit=0 confirmed. main.tex skeleton in doc well-formed (documentclass/begin+end document/input table/includegraphics/cite matched to bibitem; natbib NOT loaded). All temp files cleaned (/tmp/lab8_* removed).

- NO SECRETS: regex scan (api_key/password/secret/token/AKIA/sk-) -> none; SEED=20260815 is a fixed illustrative int, keys shown only as os.environ / .gitignore'd .env per Lab 7. Three [CHECK] tags, all honest non-fabrications: (1) AEA document-class + aea.bst exact filenames vs the template the camp distributes; (2) AEA template URL; (3) book/capstones/ gallery is a STUB at write time (README confirms) — referenced as the exemplar bar, read-for-seams when populated. No fabricated empirical magnitudes (−1.8 pp is the recurring Maya HMDA worked-example figure from Ch 8.5/nb8.5, presented as illustrative, not a claimed estimate). Cites: AEA author resources [CHECK], Gentzkow & Shapiro (2014), Wilson et al. (2017, PLOS Comp Bio 13(6):e1005510) — last two reused from Ch 8.5's bib.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
