# Handoff w7-lab7 — CoderAgent + lab author (Lab 7: "Your Data, Reproducibly")
- Wrote book/weeks/week-07/lab7-your-data-reproducibly.md: 4,727 prose words (5,738 incl. code skeletons; target ~4,200). Learning goals + Setup + Steps 1-6 (repo layout from template w/ data(gitignored)/src/notebooks/paper/logs/data-cards + .gitkeep; GitHub Classroom accept->clone->commit/push->instructor view; pin env via environment.yml python=3.11 + CONVENTIONS §5 stack + environment.lock.yml conda env export --no-builds; README what/why/how-to-run + per-source data card; .gitignore discipline blocking licensed data CRSP/Compustat/TRACE + secrets/.env, verified w/ git check-ignore; file PAP as annotated tag `git tag -a pap-filed` tied to Ch 7.3 hash-chain immutability + DEVIATIONS.md) + "you're done when" checklist + 5 reflection Qs + Where this connects. Ties Wk7-8: repo IS the capstone deliverable.
- VERIFIED: all shell/git commands run end-to-end in a clean /tmp repo (git init, .gitkeep scaffold, .gitignore + git check-ignore -v shows .parquet & .env ignored, git add -A stages ONLY .gitignore+.gitkeeps, git tag -a pap-filed annotated+dated, git ls-files shows no secrets/data). environment.yml parses as valid YAML. Two [CHECK] tags: (1) environment.lock.yml cross-OS solve (camp container = reference platform); (2) HMDA Data Browser vintage/snapshot date on container — both carried forward from ch7.2/lab4, not new fabrications.
- CONFIRMED NO SECRETS: regex scan for hard-coded api_key/token/password/secret string literals -> none; all keys shown only as os.environ[...] / `export FRED_API_KEY=...` placeholders / `.env` (which is git-ignored). No licensed data values. Cited Olken 2015 (JEP), WRDS licensing, CFPB HMDA, GitHub Classroom/.gitignore docs.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
