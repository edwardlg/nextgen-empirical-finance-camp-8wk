- nb4.3-rd-rdrobust-cct.ipynb built (26 cells, alt md/code, rng=default_rng(20260528), Agg backend, outputs empty); extracted code runs python3 → exit 0, incl. under strict chained-assignment + FutureWarning-as-error. Sharp DGP τ=0.6: hand-rolled triangular-kernel local-linear shows bias–variance (h=4→0.562/se0.179, h=32→0.411/se0.069); rdrobust CCT h≈13.3 robust τ̂=0.447 CI[0.195,0.699] brackets 0.6; Gelman–Imbens global-poly sweep is order-sensitive (-0.20→0.60→0.37); fuzzy demo recovers τ≈0.53 CI[0.27,0.79] with weak-IV CI blow-up sweep.
- API used: `from rdrobust import rdrobust, rdplot` (rdrobust 1.x). Defaults are bwselect='mserd' (CCT), kernel='tri', p=1; outputs are labeled pandas frames: out.coef/.ci/.se/.bws(.loc['h','left'])/.pv/.N_h/.rdmodel; read the 'Robust' row. PASS numpy arrays via .to_numpy() — passing a pandas Series with a non-default index breaks the fuzzy= code path ("length 18000, expected 6000"). rdplot(..., hide=True) returns obj with .J/.N_h/.binselect.
- rddensity NOT installed (no `rddensity` module; not bundled in rdrobust). McCrary test implemented as a transparent binned local-linear density check (one-sided density intercepts at c, z/p-value via scipy.stats.norm): clean p=0.569 (fail to reject), injected manipulation z=3.42 p=0.001 (rejects). If rddensity is later installed, swap in `from rddensity import rddensity`.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
