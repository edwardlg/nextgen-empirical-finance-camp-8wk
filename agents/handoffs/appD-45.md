# Handoff appD-45 — WriterAgent (Appendix D.4 Replication Packet Standard + D.5 Prose Style)

- D.4 (`book/appendices/D-style-guide/D4-replication-packet.md`, ~2,584 prose words / 3,275 incl. code): codifies the binding one-click packet from Ch 8.5 §5 + Lab 7. Covers repo layout (D.4.1 — annotated tree: data/raw+processed GITIGNORED, src/ run-order, notebooks/, paper/ with code-written tables|figures, README, environment.yml+lock, Makefile/config.py, logs/, data/cards/); data (D.4.2 — public ships in / licensed NEVER committed per CONVENTIONS §5, a pinned WRDS data-access script reconstructing licensed extracts with secrets via env vars + logs/pulls.jsonl content hashes = the public-vs-licensed "asymmetry of reproducibility"; data cards w/ license/redistribution field); code in run-order (D.4.3 — numbered scripts, no hidden hand-made inputs, logic-in-src/story-in-notebooks); pinned env (D.4.4 — environment.yml + machine-exact environment.lock.yml, "rebuild the lock = rebuild my numbers"); fixed named SEED (D.4.5 — single SEED=20260815 in config.py, np.random.default_rng passed explicitly, README-recorded, anti-seed-search/pap-filed tie-in); single make all/run_all entry point (D.4.6 — both Makefile + run_all.sh shapes, set -euo pipefail, code-generated never-pasted tables/figures); the `make clean && make all` honesty test (D.4.7); provenance stamp (D.4.8 — git commit, build time, python+pkg versions, seed, data snapshot); and a full GRADER'S CHECKLIST (D.4.9, ~9 grouped sections of checkboxes — the audit Prof. Gao runs at the conference). Reveal-the-trick framing ("reproducibility IS the credibility / inviting verification not trust"). Cross-refs D.1-D.3 by name at close.

- D.5 (`book/appendices/D-style-guide/D5-prose-style.md`, ~3,061 words; ran over the ~2,200 target because the required worked good-vs-bad rewrites are load-bearing): hedging vs overclaiming vs the over-hedged "mush" middle, plus the drift pattern abstract→conclusion (D.5.2); the weakest-assumption rule from Ch 8.3 §7 as a design→verb lookup TABLE w/ the "is associated with" vs "causes" vs "leads to" ("causal verb in a cardigan") distinctions + the verb-audit enforcement step (D.5.1); the one-sentence contribution as activity-vs-knowledge w/ calibrated verb (D.5.3); citing primary sources + the [CHECK]-don't-fabricate rule from CONVENTIONS §6 (D.5.4); active voice + the honest-limitations paragraph = residual-concern column into prose (D.5.5); banned phrases — "controls for endogeneity" (CONVENTIONS §4 name-the-threat), "in today's fast-paced world" (CONVENTIONS §1), plus proves/data-speak/obviously (D.5.6); and SEVEN worked good-vs-bad rewrites each tagged to its rule (D.5.7 — causal-on-observational, endogeneity dodge, conclusion drift, mush, empty opener, activity-vs-contribution). Reveal: "your prose is a promise about your evidence; the reader audits verbs against the design." Maya/Devon cast anchors reused. Cross-refs D.1-D.4. Reveal-the-trick voice, GFM+LaTeX, no emojis, no hard-coded secrets (verified: grep for password=/api_key/sk-/AKIA/Bearer/token = none; the SEED 20260815 + ${WRDS_USERNAME} env-var pattern + ~/.pgpass note are the only credential-adjacent items, all illustrative/safe).

- [CHECK]: D.1-D.3 siblings (table craft / coefficient reporting + FE-cluster disclosure / robustness + threats-and-responses table) do NOT yet exist at write time — only README.md stub in the dir — so both files cross-reference them BY NAME per the role split given in Ch 8.3 §8 (D.1 layout/notes, D.2 which coeffs + FE/cluster/sample disclosure, D.3 robustness/threats table, D.4 packet, D.5 prose). Reviewer should reconcile section anchors/labels once D.1-D.3 land. The 6-part packet list here (layout/data/code/env/SEED/run_all) intentionally matches Ch 8.5 §5's six parts and the SEED=20260815 + config.py + Makefile/run_all.sh patterns are kept byte-consistent with Ch 8.5 so the chapter's forward-ref to "the Appendix D packet standard" resolves cleanly. No new citations introduced (D.5 references CONVENTIONS §6 rule and Petersen 2009 only as an illustrative cite-the-primary-source example — verify Petersen 2009 clustered-SE attribution against a verified bib, though it is standard/uncontroversial). Illustrative magnitudes (Maya's 1.8pp denial gap, "12%" literacy-savings, BTC ETF "fell by a third") are worked-example figures consistent with Ch 8.3/8.5, not claimed estimates.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
