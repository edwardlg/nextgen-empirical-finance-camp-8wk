# Handoff w7-ch72 — WriterAgent (Ch 7.2: Data Acquisition in Practice)

- Wrote book/weeks/week-07/ch72-data-acquisition-in-practice.md, **6,452 words** (target ~5,600; ran long but dense, consistent w/ accepted Week-6 over-tolerance). Opens by mapping the 5-student cast (Maya/HMDA+FRED, Devon/EDGAR+crypto, Priya/Compustat+10-K, Sam/CRSP+yfinance, Leah/PatentsView) from Ch 7.1 to their pulls. Three cross-cutting rules first (secrets→env, licensed-data-stays-on-GMU per CONVENTIONS §5, pin/cache/log) w/ a reusable `log_pull` JSONL helper logging query+row count+content SHA-256 (no secrets). Then 6-field treatment (coverage·identifiers·access path·licensing+GMU rule·rate limits/gotchas·reproducible pull) for each of: WRDS Cloud (CRSP PERMNO/Compustat GVKEY/IBES/TRACE, wrds pkg, read-only, RET vs RETX + delisting-return traps), SEC EDGAR (CIK/accession, edgartools+requests, full-text/XBRL companyfacts, fair-access User-Agent rule + 10 req/s limit), FRED (series IDs, pandas-datareader/fredapi, ALFRED vintage/revision look-ahead gotcha), HMDA (CFPB Data Browser API, LEI/tract, aggregate-server-side-or-die, 2018 schema drift), 13F (EDGAR public vs Thomson/WRDS licensed, CUSIP licensing caveat, long-only/45-day-lag/confidential-treatment limits), USPTO PatentsView (API+bulk, disambiguation uncertainty + grant lag), and free alts yfinance/Stooq/Tiingo (survivorship + point-in-time caveats, match-source-to-standard). Closes w/ an 8-step defensible-pull skeleton + repo layout (.gitignore excludes data/raw/ & .env), points to data-cards/ in Appendix C throughout, and ends w/ "Your Turn" → nb7.2 + 3 reflection prompts. Does NOT cover merging/cleaning (deferred to Ch 7.4 as instructed). Ties to Ch 1.3 measurement error & Ch 6.5 audit logging.

- [CHECK] (1): line ~320 — PatentsView API base URL/auth (`search.patentsview.org/api/v1/`, key required) has changed across versions; flagged to confirm in the data card / nb7.2 before a big run. No fabricated empirical magnitudes; SEC 10 req/s, FRED ~120 req/min are documented limits. All access patterns are illustrative; nb7.2 is the runnable, verified version.

- CONFIRMED NO HARD-CODED SECRETS: every key reads from os.environ (FRED_API_KEY, WRDS_USERNAME, SEC_USER_AGENT, PATENTSVIEW_API_KEY, TIINGO_API_KEY); the only quoted key-literal in the file is the explicit anti-pattern `api_key = "abc123..."` in Rule 1, shown as what NOT to do. set_identity uses an env var or an illustrative name+email (the SEC's required User-Agent, not a secret). log_pull strips keys from the logged query string.
