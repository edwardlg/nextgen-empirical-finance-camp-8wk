# Handoff w4-ch41 — WriterAgent (Ch 4.1: Difference-in-Differences & Event Studies)

- Word count: 6,343 (`wc -w`), target ~6,600 — within range. File: `book/weeks/week-04/ch41-did-event-studies.md`. FOUNDATION DiD chapter for Week 4 (ch 1 of 5); reveal-the-trick structure; anchored on Priya's climate-insurance shock (one state changes a wildfire-pricing regulation; $2\times2$ premium table: treated 1500→1680, control 1200→1310, double diff = ATT = \$70). Builds explicitly on Week 3: reuses $Y_{it}(1),Y_{it}(0),D_{it}$, observation rule, ATT, missing counterfactual, Fundamental Problem; contrasts parallel trends vs Week-3 selection-on-observables (DiD allows a FIXED unobserved level difference, forbids a DIFFERENTIAL TREND — the relaxation from "no unobserved level diff" to "no unobserved trend diff"). Inference section reuses Week-2 (Ch 2.4) Moulton factor, block-diagonal $\boldsymbol\Omega$, cluster-robust SE, few-clusters caveat, wild cluster bootstrap. Ends by teeing up Ch 4.2 staggered adoption (TWFE $\beta$ breaks; already-treated used as controls; possible negative weights/wrong sign).
- Terms/notation DEFINED here (Ch 4.2–4.5 should reuse, not redefine): **2×2 DiD** (treated×control × before×after, 4 cell means); $\widehat{\text{DiD}}$ = double difference (shown symmetric, both orderings = \$70); DiD as potential-outcomes estimand = **ATT in post period** $\mathbb{E}[Y_{it}(1)-Y_{it}(0)\mid\text{treated,post}]$ with imputed counterfactual = own-pre-level + control's change; **parallel-trends assumption** (boxed, stated in PO and English; UNTESTABLE for post-period — counterfactual $Y(0)$ of treated group); **TWFE regression** $Y_{it}=\alpha_i+\lambda_t+\beta D_{it}+\varepsilon_{it}$ (BOXED, carried forward; $\alpha_i$ unit FE = cross-group differencing, $\lambda_t$ time FE = cross-time differencing; in 2×2 $\beta$ = DiD = ATT); equivalent $\text{Treat}\times\text{Post}$ interaction form ($\beta_3$ = DiD); **event study / leads-and-lags / dynamic** spec $Y_{it}=\alpha_i+\lambda_t+\sum_{k\neq-1}\beta_k\mathbb{1}\{t-t_i^*=k\}+\varepsilon_{it}$; **event time** $k=t-t^*$; leads ($k<0$) = pre-trend test, lags ($k\geq0$) = dynamic effect; **normalization to $k=-1$** (drop one dummy for collinearity; $\beta_{-1}=0$ by construction); **pre-trends ≠ proof of parallel trends** (logical: leads are pre-period/actual world, assumption is post-period/counterfactual; statistical: LOW POWER — noisy data → falsely reassuring flat leads; sensitivity analysis mentioned); inference = **cluster by treatment unit (state)**; few-treated-clusters problem (Priya has 1 treated state; C&K had 1) → placebo/permutation inference. Full empirical-spec sentence given for Priya's design.
- [CHECK]: none. Citations by NAME only (reviewer to finalize pages): **Card & Krueger (1994)** NJ/PA minimum-wage fast-food DiD; **Bertrand, Duflo & Mullainathan (2004)** serial-correlation placebo test (~45% false-positive rate, fixed by clustering by unit) — both standard attributions; Cameron-Gelbach-Miller (2008) wild cluster bootstrap referenced (already cited in Ch 2.4). Numbers are illustrative/self-consistent, not script output: 2×2 cells → \$70 ATT; §4.1.3 failure example (\$160 counterfactual → true ATT \$20, \$50 overstatement); §4.1.5 dynamic path 30/55/70; §4.1.7 SE table (classical 14 / HC1 16 / clustered 38; t 5.0/4.4/1.8) explicitly flagged illustrative, live numbers deferred to nb4.1. "Your Turn"→nb4.1 (notebooks/week-04/nb4.1-event-studies-parallel-trends.ipynb): build 2×2 + verify = TWFE β; event study w/ leads-lags normalized to k=-1; break parallel trends via tunable differential pre-trend; reproduce BDM placebo across SE flavors. 3 check questions (2×2 arithmetic+counterfactual+bias direction; event-study normalization + pre-trend caution; BDM serial correlation + clustering + one-treated-cluster limitation). Did NOT cover staggered critique (4.2), RD (4.3), synthetic control (4.4), shift-share (4.5) per scope.
