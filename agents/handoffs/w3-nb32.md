# Handoff: nb3.2 — PSM with Balance Diagnostics

- Built `notebooks/week-03/nb3.2-psm-balance-diagnostics.ipynb` (24 cells, 12 md + 12 code, alternating; valid nbformat, outputs empty). Reproduces the chapter worked example with `rng=np.random.default_rng(42)`: true ATT 0.318, naive 0.630, selection bias 0.311 (exact decomposition), PSM ATT 0.317 (|PSM-truth|=0.0017); SMDs collapse credit +0.72→0.00, income +0.50→0.00, age +0.25→+0.005, ps +0.97→0.00. Includes Love plot, overlap histograms (common support 0.995), and 3-bar truth/naive/PSM summary.
- Propensity model uses **statsmodels.Logit** (not sklearn). sklearn 1.8.0 IS installed but per task/chapter convention the logit is fit unpenalized via statsmodels; a code comment notes sklearn would give equivalent scores. KD-tree (scipy.cKDTree) does 1:1 NN matching with replacement under a 0.2·SD caliper.
- Verified: extracted code → temp .py → `python3` exits 0 (also clean under `-W error::FutureWarning -W error::DeprecationWarning`); all asserts pass (PSM beats naive and within 0.05 of truth; every post-match SMD<0.1). "Your Turn" injects unobserved confounder (motivation): PSM ATT 0.44 vs true 0.26 (bias +0.18) while observed SMDs all <0.05 but hidden motivation SMD stays +0.65 — CIA fails silently. matplotlib Agg, no chained indexing, .copy() on all slices. Temp files cleaned.
