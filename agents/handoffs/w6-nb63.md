# Handoff w6-nb63 — CoderAgent (nb6.3: Loughran–McDonald sentiment pipeline)

Built `notebooks/week-06/nb6.3-loughran-mcdonald-sentiment.ipynb`: 30 cells (19 markdown book-voice + 11 code, alternating), outputs empty, valid nbformat v4, `matplotlib.use("Agg")`, `rng=np.random.default_rng(42)` (used only for the seeded held-out shuffle). Offline & no licensed data: a 15-sentence in-notebook synthetic 10-K/8-K corpus labeled `truth` in {negative(6), benign_finance(6), positive(3)} — benign_finance rows are accounting prose stuffed with general-English-"negative" words (liability/tax/cost/depreciation/vice/crude/capital) that are neutral in finance. Sections: (1) corpus; (2) bag-of-words scorer from scratch (regex tokenize -> Counter; NegScore = neg-list words / total words; order-invariance sanity check); (3a) two HAND-WRITTEN illustrative subsets clearly labeled NOT-the-real-lists — naive/general-English (Harvard-GI stand-in, includes the trap accounting words + general mood words incl. `bad`/`lose`) vs LM-style finance (excludes benign accounting terms); (3b) markdown-FENCED (non-executed) loader for the real full LM Master Dictionary CSV from sraf.nd.edu w/ Negative/Positive/Uncertainty/Litigious category sets; (4/4b) score both ways + quantify benign-block false-flag rate; (4 diagnostic) tally which naive words fire on benign block + benign-share; bar chart of mean NegScore by truth x dictionary; (5) tf–idf via sklearn TfidfVectorizer(norm=None, token_pattern matched to our tokenizer) -> tf-idf-weighted LM score + raw-vs-tfidf rank table, with the honest "mild on a toy corpus, earns its keep on large corpora by muffling boilerplate; can't rescue a bad dictionary" discussion; (6) 5 hand-crafted negation/conditional trick sentences scored with combined bow vocab -> bag-of-words misclassifies, framed as the Ch 6.5 embeddings/LLM bridge; (+) seeded build/held-out split showing the gap survives OOS (workflow demo, not a stat test). "Your Turn": 4 extensions (extend both dicts; write your own trap sentences; implement a within-N-words negation patch; add positive list + NetTone — seeds PS 6.3). No chained indexing (`.copy()` used), no deprecated pandas, groupby on explicit column list. Builder kept at `_build_nb63.py` to match nb6.1/6.4 sibling convention.

VERIFIED: code extracted to temp .py, run under python3, EXIT 0. Core claim reproduced and asserted in-notebook: naive general-English false-flag rate on benign-finance sentences = 100% (6/6) vs LM-style = 0% (0/6) -> "PASS: naive > LM, as the paper predicts"; 100% of naive-flagged "negativity" in the benign block is benign accounting vocabulary (tax/liability/depreciation/cost/vice/crude/capital/expense — the paper's headline shape); bag-of-words disagrees with the human on 5/5 trick sentences ("not a bad quarter" fires on `bad`); OOS split: naive 100% / LM 0% on BOTH halves. Temp files + runtime PNG cleaned; only the .ipynb (+ builder) remain. NO fabricated paper numbers (honors ch63 [CHECK]s) — results are presented as this toy corpus's, illustrative of the mechanism. Fully synthetic; data-card pointer to `data-cards/sec-edgar.md` and the real LM dictionary URL given for the live pipeline.
