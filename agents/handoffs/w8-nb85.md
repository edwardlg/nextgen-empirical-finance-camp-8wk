nb8.5 — Final Manuscript Build (Markdown/LaTeX → PDF): built at notebooks/week-08/nb8.5-final-manuscript-build.ipynb (21 cells, alternating book-voice markdown + code; valid via nbformat v4.5, outputs empty). A seeded mini-analysis (rng=default_rng(20260815), matplotlib Agg, planted ATT=-1.80pp) WRITES two artifacts into a temp build dir — an event-study figure PNG and a result table as a LaTeX `\input` snippet (no number typed by hand) — then assembles a minimal AEA-style `main.tex` (title/abstract/intro stub/`\input{tables/...}`/`\includegraphics{figures/...}`/refs). Compile is gated on `shutil.which("tectonic"/"latexmk"/"pdflatex")`: runs the engine via subprocess if present, else prints a clear "no engine — skipping, .tex ready for Overleaf/camp container" message and does NOT fail; a pandoc Markdown→PDF path is gated identically. Well-formedness of `main.tex` verified by five string checks (\documentclass, \begin{document}, \end{document}, the \input, the \includegraphics) before any compile.
VERIFIED: extracted code to temp .py and ran python3 in BOTH modes — (a) PATH scrubbed to /usr/bin:/bin so which() finds nothing → exit 0, compile cleanly skipped, figure+table generated, all 5 well-formedness checks PASS, temp dir cleaned; (b) full PATH → exit 0, PDF_BUILT=True. Headline -1.65pp reproducible across runs (fixed SEED). No leftover /tmp build dirs; builder script removed.
LaTeX engine IN THIS ENV: FOUND — TinyTeX / TeX Live 2026 at ~/.local/bin (pdflatex, latexmk symlinks) + a standalone `tectonic` binary; the gated compile produced a real PDF here, but the verification run that must exit 0 was done with PATH stripped (assume-no-engine), confirming the skip path. pandoc NOT installed. [CHECK] none.
