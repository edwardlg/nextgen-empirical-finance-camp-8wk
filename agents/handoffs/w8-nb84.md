# Handoff w8-nb84 — CoderAgent (nb8.4: reproducibility-check / fresh-env rerun)

Built `notebooks/week-08/nb8.4-reproducibility-check.ipynb`: 21 cells (11 markdown book-voice + 10 code, strict M/C alternation, opens on the "a result a stranger cannot reconstruct is not yet a result" thesis, ends on "Your Turn"), valid nbformat v4 (validate PASS), all outputs empty + execution_count None, named `SEED=20260815` (the conference date, set once and threaded via `np.random.default_rng(SEED)` into every function as an explicit `rng` arg), `matplotlib.use("Agg")`. Self-contained OFFLINE synthetic data, no licensed/network access. Structure maps 1:1 to the §1-§7 task spec and Ch 8.5 §5 / Lab 7: (1) the analysis as four functions — `simulate_raw`(AR(1) momentum panel, true β1=0.15) → `build`(within-firm backward `shift(1)`, no look-ahead, `.copy()`) → `estimate`(numpy lstsq OLS, returns tidy dict) → `make_figure`(scatter+fit to PNG, `plt.close`), wired by `pipeline(seed)`; (2) DETERMINISM: two seeded passes, `assert res1["beta1"]==res2["beta1"]` (bit-identical float) AND `assert content_hash(res1)==content_hash(res2)` (SHA-256 of canonical sort_keys JSON), then the UNSEEDED bug — `simulate_raw_UNSEEDED()` with a fresh OS-seeded `default_rng()`, run twice, `assert bad1!=bad2` to make the disease visible; (3) `run_all(outdir, seed=SEED)` one-click entry point writes results.json + analysis.csv + figure.png to a TEMP dir + a `manifest.json` content-hash manifest (per-file SHA-256 of on-disk bytes + seed + headline_beta1); (4) FRESH-ENV discipline — described `conda env create -f environment.lock.yml` + `make clean && make all`, then SIMULATED it: `shutil.rmtree` the first run (make clean) → `run_all` into a brand-new dir (make all on a clean machine) → `assert manifest2["outputs"]==manifest1["outputs"]` from-scratch reproduction; plus a drift-detector sub-section that appends a stray byte to analysis.csv and `assert drift==["analysis.csv"]` to prove the manifest pinpoints *which* file changed; (5) `provenance_stamp(SEED)` records python + numpy/pandas/matplotlib/scipy versions + platform + seed to provenance.json. Scratch tree `shutil.rmtree`'d at the end (`assert not exists`). Requirements met: named SEED, Agg backend, `.copy()` on the one slice, no chained-index assignment, no deprecated pandas, no `df.append`.

VERIFIED: extracted 10 code cells to temp .py, ran under python3 — EXIT 0. Confirmed LIVE: seeded β1=0.16117314568012983 bit-identical across two runs with matching 64-hex results hash; unseeded runs differ (Δ≈0.002); run_all wrote 4 files + manifest; make-clean/make-all from-scratch rebuild reproduced the manifest byte-for-byte; drift detector flagged exactly analysis.csv. EXTRA robustness check: ran `run_all` in two SEPARATE python processes and confirmed `figure.png` hashes identically (`0429f9f5...`) — matplotlib 3.10 PNGs are deterministic by default here, so the "fresh-env rebuild reproduces the manifest" claim holds cross-process (the real reviewer case), not just within one kernel. Cleaned my temp builder + all `/tmp/nb84_*` `/tmp/pngchk_*` scratch dirs; only the .ipynb remains (note: unlike sibling nbs I did NOT leave a `_build_nb84.py` scaffold — the deliverable .ipynb is self-contained). Note for reviewer: the unseeded-differs `assert` is a `!=` (inequality) by design — the one place in the camp where a passing test proves a bug exists; the "Your Turn" extends to a seeded bootstrap CI (the literal `[1.2,2.4]` vs `[1.3,2.3]` Ch 8.5 failure), a real Makefile, raw-input hash-pinning, and a break-it-on-purpose exercise.
