# Handoff — nb3.1 Potential Outcomes & Selection Bias (CoderAgent)

- Built `notebooks/week-03/nb3.1-potential-outcomes-selection-bias.ipynb` (24 cells, alternating markdown/code, reveal-the-trick "play God" voice): generates the FULL science table (both Y0,Y1 per unit) with savviness-driven Y0 and constant tau=0.07 (so ATT=ATE), induces logistic self-selection on savviness (D correlated with Y0), and verifies the master decomposition naive = ATT + selection bias EXACTLY (to 1e-9) using the hidden E[Y0|D=1] counterfactual column. Reproduces the chapter's Maya binary numbers (0.621/0.549/0.409 -> ATT 0.07 + bias 0.14 = naive 0.21) in a separate binary world. Sweeps selection strength gamma in {0..3}: bias ~0 at gamma=0, grows monotonically, decomposition holds at every gamma. Randomization (fair coin on the SAME population) collapses selection bias to +0.0016 and naive difference recovers ATE (0.0716 vs 0.07). "Your Turn": flip the selection rule to gamma=-1.2 (caseworker steers low-savviness in) -> bias goes NEGATIVE (-0.1875), naive UNDERstates 0.07; predict sign/size before running. TRY-NEXT stubs: heterogeneous effect (ATT!=ATE), SUTVA spillover-breaker.
- Verified: code extracted to temp .py, ran under python3 -> EXIT 0, all in-notebook asserts PASSED; .ipynb validates via nbformat with total_outputs=0; rng=np.random.default_rng(42), matplotlib Agg, all assignment helpers return df.copy() (no chained-indexing mutation), no pd.append / no inplace. Temp builder+runner files cleaned; only the .ipynb remains.
- Notation reused verbatim from Ch 3.1 handoff (NOT redefined): Y_i(0),Y_i(1), D_i, observation rule Yobs=D*Y1+(1-D)*Y0, tau_i, ATE, ATT, naive difference Delta, selection bias = E[Y0|D1]-E[Y0|D0], (Y0,Y1) ⟂ D under randomization -> bias=0 and ATT=ATE. Maya is the running case (financial-literacy program / loan approval). Continuous "approvability" scale used for the main lab; binary approval used only to hit the chapter's exact rates.
