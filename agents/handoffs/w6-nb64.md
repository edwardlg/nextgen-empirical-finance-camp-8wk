# Handoff w6-nb64 — CoderAgent (nb6.4: Mortgage-disparity decomposition on HMDA, Bartlett/BHR-style)

Built `notebooks/week-06/nb6.4-mortgage-disparity-decomposition.ipynb`: 24 cells (14 md book-voice + 10 code, strictly alternating for the first 20; tail is 4 consecutive md cells because the real-HMDA pull is a NON-EXECUTED markdown code block per Lab-4 Path-A convention). Valid nbformat v4, outputs empty, `rng=np.random.default_rng(42)`, `matplotlib.use("Agg")`, statsmodels OLS (linear probability model so coefs read in pp), no chained indexing (every group split uses `.copy()`), no deprecated pandas. Self-contained seeded synthetic HMDA-like loan-level DGP (N=60k): `minority` indicator; risk factors income/LTV/DTI/credit-score that genuinely differ by group for upstream reasons; latent-risk threshold denial with a TUNABLE `DISCRIM_TRUE=0.80` direct-discrimination knob; plus a downstream `offered_rate` mediator carrying a steering markup. Sections: (1) DGP + spec; (2) raw gap; (3) controls march across columns; (4) Blinder–Oaxaca explained-vs-unexplained identity; (5) drop-the-score OVB (ties to Week 2); (6) over-controlling trap on the rate; (7) one decomposition figure; (8) human vs race-blind/algorithmic rule (BHR AUS, fixed noise draw so only the group penalty changes); (9) machine-checked verification (8 asserts); (10) NON-EXECUTED CFPB Data Browser pull recipe (notes HMDA has NO credit score → residual is an upper bound); (11) ties to Gao & Sun (2019, PNAS) + Capstone 1; "Your Turn" = 4 extensions (sweep the knob; worsen/improve OVB; add a 2nd HMDA-missing control; over-control on a `steered` flag).

VERIFIED: code extracted to temp .py, run under python3, exit 0; all 8 verification asserts PASS; temp files + build script + runtime PNG cleaned. Numbers @seed42: raw gap +43.92 pp → residual +21.63 pp with full risk controls (Oaxaca: 51% explained / 49% unexplained, identity exact to 1e-6); dropping the score INFLATES the residual to +32.89 pp (+52%, the OVB-flattering direction, since minority mean score is −29 FICO and B_SCORE<0); over-controlling on `offered_rate` COLLAPSES the residual from +20.85 to −5.97 pp (wrong sign — discrimination regressed away via the mediator); race-blind rule cuts the residual +20.97→+1.33 pp (raw gap does NOT vanish — legitimate risk still differs).

Notes for reviewer/Mentor: (a) The race-blind residual lands at +1.33 pp, not exactly 0 — this is a genuine linear-probability-vs-nonlinear-threshold misspecification artifact on two groups with different risk distributions, flagged honestly in §8 prose and in the Your-Turn #1 note (residual has a small nonzero floor at knob=0; checks test a >85% reduction and <2 pp absolute, not exact zero). A logit/probit Oaxaca (Fairlie-style) would tighten this floor if a future revision wants it. (b) No licensed/network data; fully synthetic and offline. (c) The §10 HMDA recipe is markdown-only and uses the CFPB `view/csv` endpoint pattern with a [CHECK] on vintage/param names, consistent with Lab 4's Path-A [CHECK]. (d) One PNG (`nb6.4-disparity-decomposition.png`) is written to cwd at runtime — not committed.
