# Handoff w4-nb45 — CoderAgent (nb4.5: Bartik / shift-share instrument decomposition)

Built `notebooks/week-04/nb4.5-bartik-shift-share.ipynb`: 26 cells (13 markdown book-voice + 13 code, alternating), outputs empty, valid nbformat v4, `rng=np.random.default_rng(20260528)` (same seed as ch4.5 code block so numbers match prose), `matplotlib.use("Agg")`, `linearmodels.iv.IV2SLS` throughout. R=400 regions × J=20 industries; Dirichlet-style shares S (gamma shape=0.5 → sparse-ish, rows sum to 1) drawn INDEPENDENT of confounder in the clean baseline; shifts g~N(0,0.05); B=S@g standardized; endogenous local_shock = 1.0·B + 0.8·confounder + noise; outcome Y = -1.5·shock + (1.0·confounder + noise). Sections: (1) build+visualize world (shares heatmap + B-vs-confounder blob), (2) OLS bias, (3) 2SLS with B + first-stage F, (4) EXACT GPSS Rotemberg weights α_j = g_j·Cov(s_rj,D)/Σ_k g_k·Cov(s_rk,D) — verified per-industry β_j Wald-ratio closed form == IV2SLS loop and Σα_j·β_j reassembles 2SLS to 6 dp; (5) STRESS demo tying the 3 biggest-|g| industries' shares to the confounder via exp(1.6·conf) → suspect shares earn biggest weights; (6) BHJ effective-#-shocks (inverse-HHI of |α_j|) across clean/stressed/concentrated regimes + AKM shock-level resampling vs vanilla robust SE. "Your Turn": 4 extensions (tie a LOW-shift share to confounder & note weak drift since weight∝shift; sweep contamination loading vs flat F; drop-the-suspect robustness; effective-shocks vs AKM-gap across J∈{5,20,80}).

VERIFIED: code extracted to temp .py, run under python3, exit 0; temp files + stray /tmp PNGs cleaned. Reproduces ch4.5 targets EXACTLY: OLS=-1.060 (biased toward zero — both confounder channels positive, masking the true negative effect), 2SLS=-1.500 (=true β), first-stage F=571.5, Σα_j=1.000000, reassembled Σα_j·β_j=-1.499902 == 2SLS to 6 dp. STRESS: suspect industries [0,14] take top-2 weights (93.6% of total |weight|), their own β_j biased toward zero (-1.04,-0.95 vs -1.5), headline drifts -1.5→-1.252 while F stays 555 (the "F can't see exclusion failure" lesson). Effective-shocks: clean n_eff=7.22/20, stressed 3.21/20, concentrated 4.43/6. AKM: dispersed-baseline shock/vanilla SE ratio 0.82 (mild), concentrated J=6 ratio 3.39 (vanilla SE far too small) — the AKM over-rejection point in code.

Notes for reviewer/Mentor: (a) Implements the EXACT GPSS Rotemberg formula, resolving ch4.5 [CHECK] item #4 (chapter's inline block used a hedged proxy α_j ∝ g_j·Cov(s_rj,shock); identical up to normalization and reassembles exactly here). (b) AKM "robust SE" is illustrated via a shock-level resampling (perturb g) bootstrap SD, NOT a packaged AKM estimator — pedagogically faithful to BHJ "shifts are the random object" and shows the vanilla-SE-too-small direction; flag if a formal AKM/ssaggregate-style SE is wanted later. (c) In the stress run, one suspect industry (idx 7) gets a near-zero share-treatment covariance → wild β_j≈-10 at rank-6 with tiny weight; harmless (decomposition identity still exact) and actually a nice realistic artifact, but reviewer may want a sentence flagging unstable single-share Wald ratios. (d) No licensed data; fully synthetic. Three PNGs written to cwd at runtime (nb4.5-world/-rotemberg-clean/-rotemberg-stress) — not committed.
