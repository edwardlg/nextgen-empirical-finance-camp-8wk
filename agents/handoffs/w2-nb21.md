- Built notebooks/week-02/nb2.1-ols-from-scratch.ipynb (26 cells, alternating book-voice markdown + code; rng=default_rng(42), Agg backend; outputs empty).
- Covers OLS β̂ via np.linalg.solve on the normal equations (with a solve-vs-inv explanation), H/M symmetry+idempotency, X'e≈0, R², leverages h_ii; reproduces Sam's β̂=(1, 7/6) and validates against statsmodels to ~1e-16; near-collinearity condition-number blow-up + Your Turn (add regressor / force singular).
- Verified: extracted code cells run python3 → exit 0, all assertions pass; temp files (build script, run file, figure) cleaned up.
