# Handoff: nb6.5-ai-copilot-lab.ipynb (CoderAgent)

- Built `notebooks/week-06/nb6.5-ai-copilot-lab.ipynb` = 38 cells (19 markdown + 19 code), alternating book-voice prose and code; covers provider-switch design, 45-row hand-labeled 8-K dataset (stratified dev/held-out-test split), offline TF-IDF RAG demo with cited grounding + cite-or-decline, local `classify()` (BoW logreg + keyword backstop), OOS harness (confusion matrix + per-class/macro precision/recall/F1 + Cohen's kappa + bootstrap CI), JSONL audit log, date-filter leakage demo, and env-gated Anthropic (prompt caching, cache_read_input_tokens) + GMU Azure real-API cells, plus a "Your Turn".
- Offline verification: extracted code to temp .py and ran with python3 and NO env keys (ANTHROPIC_API_KEY/AZURE_OPENAI_KEY/OPENAI_API_KEY unset) -> EXIT 0. Local classifier ran (test macro-F1 ~0.86, kappa ~0.80), confusion matrix + precision/recall/F1 + kappa all computed, both real-API blocks printed "skipped offline" and did NOT execute. Separately confirmed with mocked SDKs that the gated blocks DO open when keys are present (no network). nbformat validates; outputs empty; rng seeded (42); matplotlib Agg; no chained indexing / no deprecated pandas.
- Secrets: grep for key patterns (sk-, sk-ant-, Bearer, inline api_key=) found NONE; keys only read via os.environ; both live call sites gated behind `if os.environ.get(...)`. Temp files cleaned. Azure deployment/api_version marked illustrative/[CHECK].
