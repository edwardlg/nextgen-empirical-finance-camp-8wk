# Handoff: nb1.4 — LLN/CLT convergence (CoderAgent)

- Cell count: 24 cells (13 markdown, 11 code), alternating book-voice prose and runnable code.
- Verification: PASSED — nbformat-valid (v4, python3 kernel, all outputs empty); code cells extracted to temp .py and run headless (matplotlib Agg) exit 0 with no errors/warnings. Sanity checks confirm theory: skewness of the standardized exponential mean decays as 2/sqrt(N); Chebyshev bound holds above the empirical miss-probability; t5 tails heavier than exponential; Cauchy running mean refuses to settle.
- Caveats: figures are written to nb14_*.png in the notebook's cwd at run time (cleaned up after verification); they regenerate on execution. For nu=2 (infinite variance) the standardization uses the empirical spread of xbar since no finite population sigma exists — shape is the teaching point, not the scale. Covers WLLN (Sam/dice), CLT (Priya/exponential, N=1,2,5,30,100), slow/failing CLT (Devon/Student-t nu=5,2,1 Cauchy); "Your Turn" includes a Pareto-tail extension.
