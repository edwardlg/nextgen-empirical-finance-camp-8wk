# Handoff w3-ch32 — WriterAgent (Ch 3.2 Selection on Observables I: Matching & Propensity Scores)

- Word count: ~5,780 (`wc -w` = 5,776; plus two runnable code blocks + heavy display LaTeX, so effective length sits at the ~6,400 target, matching the Week 2 chapters' wc-vs-target gap). File: `book/weeks/week-03/ch32-matching-propensity-scores.md`. ALL numbers verified by running the code: Maya financial-literacy/loan-approval DGP (seed 20260528, N=4000) gives true ATT=0.322, naive diff=0.629, selection bias=0.307, PSM ATT=0.311 (text says "about 0.31"); balance SMDs credit +0.79→−0.03, income +0.45→+0.02, age +0.27→+0.05, score +1.02→0.00 — all printed verbatim into the chapter's balance table. Two-cell exact-matching hand example also verified: naive gap 0.242, ATT 0.1375, selection 0.104. Chapter code block runs end-to-end on the pinned stack (numpy 2.4.3 / pandas 2.2.3 / statsmodels 0.14.6 / scipy 1.17.1 / sklearn 1.8.0); uses `scipy.spatial.cKDTree` + `sklearn.LogisticRegression`.
- Terms DEFINED here (later chapters reference back, do not redefine): Conditional Independence Assumption (CIA) / unconfoundedness / selection on observables {Y(1),Y(0)}⊥D|X; overlap / common support 0<e(X)<1; strong ignorability (CIA+overlap); within-cell counterfactual argument for ATT; region of common support; exact matching; curse of dimensionality (12,000-cell count); propensity score e(X)=Pr(D=1|X); Rosenbaum–Rubin (1983) reduction + balancing-score proof (X⊥D|e(X)); estimating ê by logistic regression (judged by balance not predictive fit — perfect prediction = overlap catastrophe); nearest-neighbor matching; 1:1 vs 1:k; with/without replacement; caliper (0.2·SD rule, Austin 2011); bias–variance trade-off across all three knobs; PSM 4-step workflow; standardized mean difference (SMD, |SMD|<0.1 threshold, why NOT a t-test); Love plot (named, built in nb3.2); the design/outcome firewall ("check balance, never the outcome"); why ATT (not ATE) is matching's natural target. USED-not-redefined from prior chapters: potential outcomes Y(1),Y(0), D, fundamental problem, observed-outcome switching equation, ATT, ATE, selection-bias decomposition E[Y(0)|D=1]−E[Y(0)|D=0] (Ch 3.1); OVB formula β₂δ₁ + two-sign rule, zero-conditional-mean E[ε|D,X]=0, functional-form misspecification, specification-search/multiple-testing pathology (Ch 1.5 / 2.2 / 2.5). Forward-refs (keep accurate if scopes shift): Ch 3.3 reweights the SAME score — IPW, entropy balancing (Hainmueller), AIPW/doubly-robust — explicitly set up at end of 3.2.4 and in 3.2.9; deliberately did NOT cover IPW/AIPW. Ch 3.4 + Week 4 (IV, natural experiments, RD/DiD) named as the escape when CIA is not credible. Rosenbaum sensitivity analysis mentioned by name as "advanced reading," not developed.
- [CHECK]: (1) Citations are in-text mentions only, standard and confident but NOT bibliographically verified against a reference list — Rosenbaum & Rubin (1983, Biometrika, propensity score + balancing-score theorem), Austin (2011, the 0.2-SD caliper and the SMD<0.1 balance convention), Stuart (2010, matching review). Verify venue/volume before any formal bibliography. (2) Ch 3.1 IS NOT YET WRITTEN (week-03 dir was a stub; only README present) — I anchored to its planned content per TOC.md lines 119/126/133 and the spec (potential outcomes, SUTVA, ATE/ATT/LATE, selection-bias decomposition). When Ch 3.1 is drafted, confirm it actually defines the switching equation Yᵢ=DᵢYᵢ(1)+(1−Dᵢ)Yᵢ(0), ATT, and the exact selection-bias decomposition I reuse verbatim; reconcile if 3.1's notation drifts. (3) All empirical numbers are from the invented illustrative DGP, fully reproducible — no licensed-data claims. nb3.2 (`notebooks/week-03/nb3.2-psm-balance-diagnostics.ipynb`) pointed to from Your Turn; CoderAgent owns it — chapter code block is a clean seed that scales to a real HMDA/program-enrollment panel, adds the Love plot, and includes the unobserved-confounder (motivation) failure demo. 3 check questions included.
