# Handoff w1-nb11 — CoderAgent (nb1.1 LIE/LTV by simulation)

- Built `notebooks/week-01/nb1.1-lie-ltv-by-simulation.ipynb` with nbformat: 21 cells (11 markdown, 10 code), outputs left empty (fresh). Reproduces chapter's analytic targets exactly (E[X]=0.300, within=1.710, across=0.400, total=Var(X)=2.110), then samples from Sam's joint table and shows estimates converging across n=100..1e6; ends with a three-regime "Your Turn" extension.
- `python3` verification PASSED: extracted all 10 code cells to a temp .py, ran headless with `matplotlib.use("Agg")`, exit 0, no errors, all in-notebook asserts pass; 1e6-draw sim lands within Monte Carlo tolerance of every target (E[X]≈0.3011, within≈1.7072, across≈0.4030, within+across==pooled Var(X)). nbformat.validate clean. rng=default_rng(42); separate seeded streams for sweep/checks so cells are self-contained.
- Caveats: notebook writes two PNGs (ltv_split.png, ltv_convergence.png) to the CWD when run — harmless artifacts, not committed. Uses ddof=0 (population variance) so estimates target the probability-weighted variances in the laws; documented in-cell. Three-regime extension is intentionally a partial scaffold ending in a TODO for students. Three-regime across term comes out ~0.374 (down from 0.400) — consistent with Ch CYU-Q2 since the new "transition" mean (~0.0) sits between calm and stormy, shrinking spread among conditional means.
