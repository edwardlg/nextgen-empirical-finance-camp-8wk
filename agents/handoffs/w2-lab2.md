# w2-lab2 — Lab 2: Replicate a Textbook Fama–MacBeth on CRSP

Word count: 4,720 (target ~4,200; over due to two full code paths + reflection/extension scaffolding — prose body on target). Two-path structure per CONVENTIONS §5: Path A (real WRDS/CRSP) shows the `wrds.Connection` pattern, the `crsp.msf` + `ff.factors_monthly` query, a PINNED snapshot (CRSP_SNAPSHOT="2025-12-31"), the licensed/read-only-on-Hopper note, and NO CRSP numbers; Path B is the self-contained seeded synthetic one-factor panel (same schema permno/month/ret/mkt_excess) with planted LAMBDA_TRUE=0.006. Full two-pass method given with the math (Pass 1 time-series betas, Pass 2 per-month cross-sectional regs, aggregate to FM mean + FM SE = sd(slopes)/sqrt(T)); FM-SE-as-time-clustering explained and tied explicitly to Petersen (2009) §6 of ch24, incl. the "FM does NOT fix a firm effect" warning + Newey-West remedy.

Synthetic block VERIFIED: extracted the exact Path B + Step 1–4 code to a temp .py, ran under python3 with matplotlib Agg, exit 0. Recovers gamma1=0.00550 (planted 0.006), FM t=3.78, corr(beta_hat,true_beta)=0.935, and pooled-OLS SE 0.000598 << FM SE 0.001456 (the lying-t-stat diagnostic). Four asserts pass. Temp files cleaned.

[CHECK] none on data (synthetic numbers reproducible from SEED=20260527). Fama & MacBeth 1973 JPE 81(3):607-636 cited; Petersen 2009 and White 1980 referenced per ch24. Reviewer note: Path A query is illustrative (shrcd 10/11, exchcd 1/2/3, ff.factors_monthly) and was not executed (no WRDS creds off-infra by design) — an editor with Hopper access should sanity-check exact table/column names against the current WRDS schema before students run it.
