nb4.2 (Staggered DiD: TWFE vs Callaway-Sant'Anna) built & verified: 29 cells (15 md / 14 code), valid .ipynb, extracted-code run exits 0 with no Future/Deprecation warnings. Reproduces chapter exactly: World A TWFE=+0.40 (sign flip; true mean=-3.4545 vs chapter -3.455); hand Goodman-Bacon decomp shows clean 2x2=-2.0, FORBIDDEN 2x2=+1.0, weights 0.20/0.80, identity 0.20*(-2)+0.80*(+1)=+0.40==beta; World B TWFE=-1.2273 (chapter -1.23) while CS overall ATT=-2.5714 (chapter -2.571) recovered to the decimal (assert <1e-6). World C is a NEW noisy 40-state/12-yr panel (rng=np.random.default_rng(20260528), cohorts 2017/2019/2021 + never): TWFE=-1.17 (64% attenuated) vs CS=-1.79 ~ true -1.82, flat pre-trends, clean building dynamics; figure nb4.2_staggered_twfe_vs_cs.png (event-study overlay + headline bars).
differences API (v0.3.0): never-treated cohort MUST be np.nan (not 0); ATTgt(data=<MultiIndex (entity,time) df>, cohort_column="G").fit(formula="Y", control_group="never_treated", progress_bar=False) -> ATTgtResult; group-time grid via res.to_pandas() (ATT col key = ('ATTgtElements','','ATT'), index cohort/base_period/time); aggregate("simple", overall=True) -> overall ATT, aggregate("event") -> event study (index relative_period, ATT col ('EventAggregation','','ATT'), SE under 'analytic'). TWFE via pyfixest feols("Y ~ D | state + t"); naive event study via i(rel_f, ref=-1) with never-treated binned to ref.
Seed/number reconciliation: Worlds A & B match the chapter's hand-built numbers EXACTLY (noiseless linear DGP, effect -(e+1)). The single isolated forbidden 2x2 cancels to 0 in that symmetric DGP, so the sign flip is shown the honest way via the full Goodman-Bacon 2-block weighting (forbidden block carries 80% weight). World C numbers differ by construction (new noisy panel) but qualitative result identical. No bacondecomp package used; GB decomposition done by hand for the 2-group case.
