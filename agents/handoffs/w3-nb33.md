# Handoff w3-nb33 — CoderAgent (nb3.3: entropy balancing vs IPW vs AIPW)

Built `notebooks/week-03/nb3.3-entropy-balancing-ipw-aipw.ipynb`: 26 cells (14 markdown, 12 code), outputs empty, valid nbformat v4.5. Reproduces the chapter's worked example to the digit on the chapter's data seed 33 (notebook-wide `rng=np.random.default_rng(42)` reserved for auxiliary use, a separate `data_rng=default_rng(33)` drives the DGP): true -0.800, naive -1.502 (biased), IPW Hajek (stabilized) -0.785, AIPW -0.779, with HT shown misbehaving (-0.727) to motivate Hajek. IPW diagnostics print max/99th-pct/effective-N, and a thin-overlap explosion demo (selection 2.5*inc+2.0*cre) shows max weight 154.5 -> 49.6 and effN 806 -> 1001 after stabilizing+trimming. Double-robustness: misspecification = dropping the confounder `income`; AIPW survives ps-good/out-bad (-0.783) and ps-bad/out-good (-0.770) but fails both-bad (-1.249), matching the single-shot IPW-only (-1.248) and reg-only (-1.249) failures. "Your Turn" verified offline: break-both = -1.25, intercept-only-both collapses exactly to naive -1.502, EB-on-credit-only leaves income imbalanced -0.79 and ATT drifts to -1.25.

Entropy balancing implemented from scratch with **Newton's method on the convex dual** (objective log sum_i q_i exp(-Z_i.lambda), Z = X_control - treated_target; gradient = weighted mean of Z, Hessian = weighted covariance of Z via np.linalg.solve) — one lambda per balance constraint, not per unit; converges in 5 steps (means) / 8 steps (means+vars) to machine precision. Asserted reweighted control means match treated means to 6.68e-17 (means) and means+vars to 5.04e-17, far inside the ~1e-6 bar; EB ATT -0.756 (means) vs -0.751 (means+vars) demonstrates the robustness-to-spec point. scipy.optimize imported as a documented cross-check path. VERIFIED: code extracted to temp .py, run under python3 with matplotlib Agg backend, exits 0 with no errors, all asserts pass; temp files cleaned. Note for editor: on this correctly-specified logit DGP, EB is mathematically equivalent to an IPW so its max control weight is comparable to IPW's (the prose says so honestly) — EB's distinct guarantee here is *exact* balance every sample, and IPW/AIPW target ATE while EB targets ATT (they coincide because tau is a constant -0.8).
