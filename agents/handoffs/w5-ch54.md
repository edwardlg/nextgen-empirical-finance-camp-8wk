# Handoff w5-ch54 — WriterAgent (Ch 5.4 Reader's Guide: Petersen 2009)

- Word count: ~3,522 (`wc -w`; target ~3,100, within the TOC's 2,800–3,400 Reader's-Guide band). File: `book/weeks/week-05/ch54-readers-guide-petersen-2009.md`. Follows the FIXED anatomy in order: (1) research question — methods paper, "which SE is right + how wrong if you pick wrong," with "reading a methods paper as its own skill" framed up front; (2) identification = NOT causal, Monte Carlo simulation (build worlds with known true β + known correlation structure, test estimated-vs-true SE and 95% coverage) + real-panel re-analysis; (3) data = simulated panels as workhorse + illustrative real panels + the firm-effect/time-effect/both taxonomy table; (4) table-by-table reading order — simulation tables first (true SE as ruler → OLS undercovers → White ALSO undercovers → cluster-by-firm matches; then flip to time-effect panel), then FM/Newey-West comparison, then guidance synthesis last; (5) what's clever — simulation-as-proof, clean taxonomy/flowchart, making subtle econometrics legible to finance, the cluster-firm-then-cluster-time self-diagnostic; (6) what's vulnerable — few-clusters (his own caveat, G→∞ not N→∞), gap to general two-way clustering (CGM 2011/Thompson 2011 came AFTER), reliance on the simulated additive variance-component structure (decay/network dependence not in his Monte Carlo); (7) three replication exercises pointed at nb5.4 (firm-effect core table w/ true-SE+coverage; flip to time effect + time FE; both-effects two-way + shrink-clusters break). Ends with explicit Week-2 Ch-2.4 §6 connection (incl. the nb2.4 t=5.70→2.19 callback), BDM-2004 forward link, nb5.4 pointer, and 3 referee questions (how-many-clusters / is-"robust"-a-trap-word / does-your-real-dependence-match-a-simulated-shape). Accuracy rule honored: well-established conclusions stated as fact (OLS & White SEs badly biased down under firm/time effect; cluster on the dependence dimension; two-way when both); NO fabricated precise table numbers — all specific figures tagged [CHECK]. Reveal-the-trick voice, no emojis.
- [CHECK] items (5, all on uncertain specifics — reviewer to verify against the actual paper): (1) §1 — the count of surveyed papers + method-breakdown + which journals (JF/JFE/RFS) in Petersen's literature survey; (2) §3 — exact simulation grid (N firms, T years, # replications S); stated only qualitatively ("hundreds-to-thousands of firms, ~10 years"); (3) §3 — which real dataset(s)/regression(s) he uses for the illustrative panels; (4) §4 — the precise estimated/true SE ratios in the core tables (said "often ~one-half or worse, t inflated by ~√2+" but flagged not to quote from memory); (5) §6 — exact attribution/dates for the general two-way estimator (Petersen's own two-way vs. Thompson 2011 vs. Cameron-Gelbach-Miller 2011). Also note: CGM (2008) wild-cluster-bootstrap cite reused from Ch 2.4 (already footnoted there, confident). nb5.4 referenced per TOC as "Petersen clustering replication" — CoderAgent owns the simulation harness; the 3 exercises assume it exposes tunable firm/time variance components + cluster-count knob (matches nb2.4 design).
