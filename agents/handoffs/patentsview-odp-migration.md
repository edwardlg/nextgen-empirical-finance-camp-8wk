- 2026-05-30: Migrated nb6.1 (cell 22 markdown + cell 23 RUN_REAL=False code) and nb7.2 (GATES dict cell 10, narrative cell 17, pull_patentsview cell 18) from `search.patentsview.org/api/v1` + `X-Api-Key` + `PATENTSVIEW_API_KEY` to ODP `https://api.uspto.gov` + `X-API-KEY` (lowercase header) + `USPTO_ODP_API_KEY` env var, mirroring the canonical snippet in `data-cards/uspto-patentsview.md`; both real-API blocks remain env-gated and live inside the clearly-marked "real-path; not executed offline" cells, so offline verification is unaffected.
- Validation: `nbformat.validate` clean for both notebooks; offline run (extracted code cells, `python3`) returns exit 0 on each, with nb7.2 reporting `PATENTSVIEW gated on USPTO_ODP_API_KEY -> absent (offline)` and the "no real source was called" assertion holding; one tight `[CHECK]` retained in each notebook on the exact post-cutover patent-search endpoint path under `${base}/api/v1/...` (USPTO is still finalizing this; data card carries the same flag).
