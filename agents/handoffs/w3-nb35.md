# Handoff w3-nb35 — CoderAgent (nb3.5: weak-IV pathology reproduction)

Built `notebooks/week-03/nb3.5-weak-iv-pathology.ipynb`: 26 cells (13 markdown, 13 code), alternating book-voice prose + code, outputs empty, valid nbformat v4, rng=np.random.default_rng(42), matplotlib Agg. DGP from ch3.5.8 with beta_true=0 so any nonzero estimate is pure bias; N=400, weak pi=0.07 vs strong pi=0.8, endog=2.0. The pedagogical spine is "a single draw can mislead" -> everything is a 1,200-rep Monte Carlo reporting averages/coverage/distributions. Hand-rolled just-identified 2SLS (projection formula) cross-checked vs linearmodels.IV2SLS to 1.67e-16. AR CIs by grid inversion over beta0 in [-10,10] (1001 pts), vectorized via the linear-in-b0 algebra trick; edge-touching flagged unbounded. Plots: first-stage-F histogram, coverage bar chart (conv vs AR x weak vs strong), 2SLS sampling-distribution overlay (tails clipped), many-IV bias line. "Your Turn" pads one weak relevant instrument with K-1 pure-noise instruments.

VERIFIED: code extracted to temp .py, run under python3, exit 0 in ~15s (well under a minute), stderr empty (no warnings). Weak case: median OLS=0.400, median 2SLS=0.255 (sits strictly between truth 0 and OLS -> retains ~64% of OLS bias), conventional 95% coverage=0.794 (<<0.95), AR coverage=0.979 (~95%), AR unbounded on 90.8% of draws, mean first-stage F=1.36 with 99.4% below 10 but MAX F=16.6 spuriously large (significance != strength), Olea-Pflueger effective F=1.24 (flagged). Strong case: 2SLS median=0.001 on truth, conventional coverage recovers to 0.940, AR=0.960, never unbounded, effective F=54. Many-IV bias: median 2SLS marches 0.259 (K=1) -> 0.400 (K=60), collapsing exactly onto OLS. All in-notebook assertions pass; temp files cleaned. Note: 2SLS is a fat-tailed ratio so the notebook leans on the MEDIAN (not the unstable mean, which printed +1.337) for the toward-OLS central-tendency claim — this is the statistically honest summary and is flagged inline for students.
