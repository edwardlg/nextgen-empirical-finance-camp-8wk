# Handoff w3-nb34 — CoderAgent (nb3.4 "2SLS with linearmodels; first-stage F")

- Built `notebooks/week-03/nb3.4-2sls-first-stage-f.ipynb` (23 cells, alternating markdown/code, outputs empty, valid via nbformat). IV2SLS import path used: `from linearmodels.iv import IV2SLS` (linearmodels 7.0; confirmed import + `iv.first_stage.diagnostics[["f.stat","f.pval"]]` and `iv.first_stage.individual["D"]` accessors). VERIFIED end-to-end (extracted code → python3 → exit 0): reproduces chapter exactly — OLS β̂=-3.623 (biased, more-negative than truth as motivation enters both D and Y's error), 2SLS β̂=-1.921 (true -2.0), first-stage partial F=286.2 (=t² on Z, strong); by-hand 2SLS == IV2SLS to 6dp (-1.9205); continuous-treatment+controls version shows by-hand == FWL cov-ratio == IV2SLS all -1.9942; weak variant (0.05*Z) blows up SE and F→~0; Monte-Carlo strength sweep median trace hugs OLS bias (-3.13) at F≈1 then converges to -2.0 past F=10. Hand-rolled second-stage SE shown WRONG vs IV2SLS (the chapter's bold warning). rng=default_rng(42) main + sub-seeds (chapter's 20260528 for the exact-numbers binary block); matplotlib Agg; no chained indexing; .to_numpy()/np.linalg.lstsq throughout.
- 3-panel plot (A: median 2SLS vs F hugging OLS bias then converging; B: SE explosion log-log; C: F vs loading π). Figure saved relative ("nb3.4-instrument-strength.png") so it lands in the notebook's run dir, not the repo. "Your Turn" = 4 tasks: flip confounder sign & re-trace bias, find the danger-zone F (fraction of CIs excluding truth), mean-vs-median pathology, add a 2nd instrument (over-identification). Teaching note: used MEDIAN (not mean) for the bias trace because weak-IV fat tails corrupt the mean — itself flagged as a Your-Turn point. Weak-IV deliberately set up, not cured → tees up Ch 3.5 / nb3.5 (Anderson–Rubin). No data deps (pure simulation); sits alongside existing nb3.1–3.3.
