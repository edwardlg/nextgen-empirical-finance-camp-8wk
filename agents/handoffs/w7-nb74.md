# Handoff w7-nb74 — CoderAgent (nb7.4: dataset-build & validation pipeline)

Built `notebooks/week-07/nb7.4-dataset-build-validation.ipynb`: 27 cells (14 markdown book-voice + 13 code, strict M/C alternation, ends on "Your Turn" markdown), valid nbformat v4 (validate PASS), outputs empty + execution_count None, `rng=np.random.default_rng(42)`, `matplotlib.use("Agg")`. Self-contained OFFLINE synthetic data — no licensed/network access. Generates 3 raw tables mimicking CRSP (PERMNO,date,ret,dlret monthly), Compustat (GVKEY,datadate fiscal-year, heavy-tailed book_equity w/ 8 negative + 5 extreme), and a time-varying CCM-style link (linkdt/linkenddt windows + linkprim P/J flag; one company gvkey 1001 has two share classes 80001+90001 to exercise the LINKPRIM filter). Flow: (1) firm universe w/ IPO entry + delisting exit so per-year count CHURNS → (2) `merge(validate="many_to_one")` link + DEMONSTRATED Cartesian explosion (unguarded 4.6x blow-up shown, then `validate="one_to_one"` in try/except CATCHES it w/ MergeError) + `indicator=True`/`how="left"` match rate (97.6%, secondary class 90001 correctly unmatched) → (3) survivorship: per-year firm-count plot (full churns 16→61→33 vs survivor-only monotone) + survivors-only mean biased UP +0.30% (asserted) by dropping delisting-return crashes → (4) 6-month FF lag via `available_date` (June 30 of y+1) + `merge_asof(direction="backward")` + no-look-ahead `assert (date>=available_date).all()` (HOLDS over 2,556 rows; concrete gvkey-1002 example: fiscal-2017 → first usable 2018-06-30) → (5) winsorize bm by date (groupby transform, 1/99, max 440→280, N preserved, asserted) → (6) missing-data groupby probe on a missingness flag (log book equity gap 0.04 SD → MCAR-consistent, matches the random-blanking construction) → (7) `step()` row-count logger printing raw→analysis audit trail, final panel saved to TEMP dir (parquet w/ CSV fallback). Requirements met: `.copy()` after every slice (8x), no chained-index assignment, no `df.append` (only list.append→single `pd.concat`, the chapter-endorsed pattern), no deprecated pandas.

VERIFIED: extracted 13 code cells to temp .py, ran under python3 with `-W error::FutureWarning -W error::DeprecationWarning`, EXIT 0. All load-bearing behaviors confirmed live: validate= catches the bad merge (MergeError raised), the look-ahead assert holds, winsorize caps the tail while preserving N, match-rate excludes the secondary share class, survivorship bias is upward. Temp build script (`_build_nb74.py`) and all `/tmp/nb74_*` scratch dirs/PNGs/parquet cleaned; only the .ipynb remains. Note for reviewer: the missingness probe is MCAR-by-construction (returns blanked uniformly at random) — the "Your Turn" #1 has students rebuild it MNAR on HMDA so the denial-rate gap widens; "Your Turn" #2 has them flip merge_asof to direction="forward" to watch the no-look-ahead assert FIRE.
